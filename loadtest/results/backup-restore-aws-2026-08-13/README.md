# 백업/복구 본 측정 (AWS) — 2026-08-13

`AWS-RIDE-ALONG.md` §1 主-**P3**. 설계는
[`backup-restore-rto-rpo.md`](../../../docs/decisions/backup-restore-rto-rpo.md),
사전 확인(게이트 G1~G4)은 [`../backup-restore-2026-08-13/`](../backup-restore-2026-08-13/README.md) 이고,
이 디렉터리는 그 rig 를 **1억 행 무대에서 EC2 무인으로 돌린 본 측정**이다.

배포 문서에 **TODO 로만 남아 있던 「백업/복구」** 를 시간(RTO)과 손실 창(RPO)으로 바꾼다.

## 0. 판정

**백업은 두 팔 다 서비스를 «사실상 안 멈춘다». 갈리는 것은 되돌리는 시간이고, 자릿수가 다르다**
(배수는 §3 때문에 인용하지 않는다).

| | **A** `mysqldump` (논리) | **B** XtraBackup (물리) | **C** 파일 스냅샷 (대조군) |
|---|---|---|---|
| 판 수 | 3 (+버림판 1) | 3 (+버림판 1) | 1 |
| 백업 소요 | 168~178s | **139~144s** | 2s |
| 산출물 | **5,682 MB** | 10,367 MB | 10,245 MB |
| **복구 소요** | **1,282~1,304s (약 21분)** | ~~8~9s~~ → **약 174s (약 2.9분)** ⚠️ §3 | 미측정(대조군) |
| **최대 쓰기 정지** | **42~126 ms** | 244~296 ms | **2,692 ms** |
| p50 쓰기 지연 | 2~4 ms | 11~13 ms | 5 ms |
| 복구 검증 | 행수·체크섬 일치 | 행수·체크섬 일치 | — |

원문 [`backup/backup.tsv`](backup/backup.tsv).

- **H1 「`--single-transaction` 은 사실상 안 멈춘다」 — 지지된다.** 팔 A 의 최대 정지가 42~126ms 다
- **H2 「물리가 논리보다 복구가 빠르다」 — 지지된다. 배수는 약 7배**(초판의 «143배» 는 철회 — §3)
- **팔 C 가 계측의 양성 대조군 역할을 했다** — 같은 rig 에서 명백히 잠그는 팔이 2,692ms 로
  찍혔다. 그래서 A 의 42~126ms 는 **「계측이 못 잡았다」가 아니라 「실제로 안 멈췄다」** 로 읽힌다.
  이 구분을 세우려고 C 를 넣었고(설계 §9-1 ②), 실제로 값을 했다

## 1. Q1·Q2 — RTO

| 질문 | 답 |
|---|---|
| **Q1** 전부 잃었을 때 다시 쓰기를 받기까지 | **팔 A 로 약 21분** (1억 행 · m6i.xlarge · gp3 200GB 기준) |
| **Q2** 논리 vs 물리 복구 시간 차이 | **약 7배** (1,282~1,304s ↔ 약 174s). 초판의 «143배» 는 철회 — §3 |

🔴 **절대 시간을 「운영에서 N분」으로 인용하지 않는다.** 하드웨어 종속이다(설계 §5). 인용할
때는 **인스턴스 타입·디스크·행 수를 같이 붙인다.**

**산출물 크기는 반대 방향이다** — 논리 덤프 5,682MB 는 물리 백업 10,367MB 의 **55%** 다.
「물리가 모든 면에서 낫다」가 아니라 **보관 비용 ↔ 복구 시간의 교환**이다.

백업 처리량으로 환산하면 팔 B 는 10,244MB / 140s ≈ **73MB/s** 로, 이 볼륨의 상한
(gp3 125MB/s)과 같은 자릿수다. **백업 쪽은 디스크가 지배한다**는 설계 §5 의 전제가 맞았다.

## 2. Q3 — 백업이 서비스를 멎게 하는가

writer 는 초당 5회(`WRITER_GAP_MS=200`)로 쓰면서 정지 구간을 200ms 해상도로 본다.

- 팔 A — **42~126ms.** `--single-transaction` 은 시작 시점에만 짧게 잡고 이후엔 MVCC 로 읽는다
- 팔 B — **244~296ms.** `LOCK INSTANCE FOR BACKUP` 이 있지만 DML 을 막지 않는다
- 팔 C — **2,692ms.** `FLUSH TABLES ... FOR EXPORT` 로 3초를 잠갔고 그대로 잡혔다

**세 팔 모두 `errors=0`** 이다. 쓰기가 실패한 게 아니라 **지연됐다가 통과**했다.

## 3. 🔴 팔 B 의 「복구 9초」는 틀린 값이었다 — **교정 완료: 약 174초**

**이 절이 이 문서에서 제일 조심해야 할 곳이다.**

`restore_arm_b` 는 ① prepare ② `cp -a` 로 datadir 붓기 ③ 서버 기동 + ready 대기를 다 감싼다
([`../backup-restore-2026-08-13/backup_sweep.sh`](../backup-restore-2026-08-13/backup_sweep.sh)).
그런데 로그를 보면 **prepare 만 3초**다(`08:48:24` → `08:48:27`,
[`backup/r2_B_xb.log`](backup/r2_B_xb.log)). 남은 ~6초에 **10.4GB 복사 + 컨테이너 기동 + ready
대기**가 전부 들어갔다.

**물리적으로 불가능하다:**

| | 값 |
|---|---|
| 복사량 | 10,367 MB (읽기 + 쓰기가 **같은 볼륨**) |
| 볼륨 상한 | gp3 200GB · **125 MB/s** · 3,000 IOPS (실측 `describe-volumes`) |
| 필요한 최소 시간 | ≈ 166초 |
| 실측(초판) | 9초 (prepare 3초 포함) |

### 원인 — 페이지 캐시가 아니라 **XFS reflink** 였다

⚠️ **이 절의 초판은 원인을 「`cp` 가 페이지 캐시까지만 쓰고 반환했다」로 적었다. 그건 틀렸다.**
그 가설로 [#201](https://github.com/Shadowfit/init/issues/201) 이 복구 직후 `sync` 시간을 재는
수정을 넣었는데, **b 라운드에서 그 값이 15~22ms 로 나왔다** — 캐시에 10.4GB 가 남아 있었다면
수십~수백 초여야 한다.

실제 원인은 **루트 파일시스템이 xfs `reflink=1`** 이라는 것이다. `cp -a` 가 extent 를 공유해서
**복사가 아예 일어나지 않았다.** 내려쓸 것이 없으니 `sync` 도 즉시 반환한 것이다.
([#210](https://github.com/Shadowfit/init/issues/210))

### 교정 측정 (2026-08-14)

같은 아티팩트로 플래그만 바꿔 다시 쟀다 —
[`../restore-reflink-2026-08-14/`](../restore-reflink-2026-08-14/README.md).

| | `--reflink=never` (교정) | `--reflink=auto` (기존) |
|---|---|---|
| `cp` | **166s** (4판 전부 동일) | 2s |
| **디스크 소비** | **10,409 MB** | **0 MB** |
| **복구 합계** | **173~174s** | 9s |
| 복구 직후 `sync` | 5~15 ms | 12~19 ms |
| 행수·체크섬 | 일치 | 일치 |

**위 표가 계산한 「≈166초」가 실측 166초로 떨어졌다.** 반증 논증이 예측까지 맞췄다.

→ **「1,294초 vs 9초 = 143배」는 철회한다. 교정값은 1,223~1,230초 vs 173~174초 = 약 7배**
([§0 표](#0-판정)의 팔 B 복구 칸은 교정값으로 갱신했다). 판정 **방향**(물리가 논리보다
빠르다)은 바뀌지 않는다 — 자릿수가 하나 줄었을 뿐이고, 이제 **두 팔이 같은 기준**(둘 다
디스크에 실제로 내려간 상태)에서 재졌다.

🔴 **검증 칸은 두 모드 다 «일치» 였다.** reflink 는 CoW 라 내용이 안 깨진다. 즉 이 결함은
「복구가 실패한다」가 아니라 **「복구 시간이 거짓말을 한다」** 이고, **표만 보면 아무 문제가
없어 보인다.**

## 4. G5 — 「복구되는가」가 닫혔다

```
원본 (행수 체크섬): 100000000 62281922
복구 (행수 체크섬): 100000000 62281922
판정: 일치
```

[`backup/G5_xtrabackup_restore.txt`](backup/G5_xtrabackup_restore.txt).

**G2 가 남긴 단서를 이걸로 닫는다.** 사전 확인에서 XtraBackup 은 `perl binary not found.
Skipping the version check` 를 찍은 채 돌았고(도구 8.0.35 ↔ 서버 8.0.46), 그때 판정은
「붙는다 ≠ 호환이 검증됐다」였다. **1억 행을 복구해서 체크섬이 맞았으므로 이제 「복구된다」다.**

본 측정 9판의 `verify` 칸도 전부 일치다. 형식은 `일치(100003365 ∈ [100002701,100003373])`
— writer 가 계속 쓰는 중이라 **백업 시점의 행수는 구간으로만 정해진다.** 그 구간 안에 들어오면
일치다.

## 5. RPO — binlog 는 어디까지 좁히나

EC2 에서도 게이트를 다시 밟았다(로컬 통과 이력을 재사용하지 않는다).

| | 값 |
|---|---|
| `binlog_format` / `binlog_row_image` | `ROW` / `FULL` |
| `gtid_mode` | **OFF → PITR 은 포지션 기반**(파일명 + 오프셋) |
| `binlog_expire_logs_seconds` | 2,592,000 (30일) = **RPO 상한** |
| **PITR** | 백업 `binlog.000036 @ 532366` → 사고 직전 **20,500행으로 정확히 복원** |

[`backup/G1_binlog.txt`](backup/G1_binlog.txt) · [`backup/G3_pitr.txt`](backup/G3_pitr.txt).

**사고(대량 DELETE)는 재생되지 않았고 그 직전까지만 돌아왔다.** RPO 의 하한은 백업 주기가
아니라 **binlog 가 살아 있는 한 「사고 직전」** 이다 — 단 binlog 가 같은 디스크에 있으면
디스크를 통째로 잃을 때 같이 잃는다(§7).

## 6. 측정 조건

| 항목 | 값 |
|---|---|
| 인스턴스 | **m6i.xlarge** (4 vCPU / 15GB) · ap-northeast-2c |
| 디스크 | **gp3 200GB · 125MB/s · 3,000 IOPS** (여유 106G) |
| MySQL | 8.0.46 · 버퍼풀 2,147,483,648 (2GiB) |
| 대상 | `pose_data_scale` **1억 행** |
| 커밋 | `7e8c686376c01eccdd94c3398e61e736c38dce8c` (`work/2026-08-13`) |
| 단계 | preflight 2s · rehearsal 261s · **backup 10,421s** · ridealong 0s · collect 0s (전부 OK) |
| 판 배치 | 팔당 버림판 1 + 본판 3, 판 사이 `DROP_CACHES=1` |
| 가동 / 러너 | **3.11h / 2.97h** · 청구액은 `MANIFEST.txt` 의 `# 요금` 칸 |

`rehearsal/` 의 수치는 **측정값이 아니다** — 경로만 밟는 축소 판(`SESSIONS` 축소)이다.

## 7. 從 항목 (인프라가 살아 있을 때만)

| | 결과 |
|---|---|
| **R1** worst-section | ✅ **이번엔 돌았다.** `reports` 0행 · `detailed_analysis` 채워진 행 0 · `pose_data` 0 · `exercise_sessions` 0 |
| **R2** MySQL 지표 | ✅ `global_status` · `global_variables` · `top_digest` 수집 |
| **R3** 3-way 조인 | 🔴 **SKIPPED** — `reports`·`exercise_sessions`·`users` 시딩이 선행 |

**R1 은 08-08·08-12 에 연속으로 놓쳤던 항목이다.** 08-12 라운드에서 「러너가 mysql 만 띄우고
Flyway 를 안 돌려 테이블 자체가 없었다」가 원인이었고, `bootstrap.sh` 에 Flyway 단계를 넣은
수정이 **여기서 효과를 확인했다** — 이번엔 「테이블 부재」가 아니라 **「테이블은 있고 전부 0행」**
이라는 답이 나왔다.

원문 [`ridealong/`](ridealong/).

## 8. 미검증 · 한계

- ✅ **팔 B 복구 시간(§3) — 해소됐다.** 원인은 캐시가 아니라 **XFS reflink** 였고
  ([#210](https://github.com/Shadowfit/init/issues/210)), 08-14 교정 측정에서 **약 174초**로
  세워졌다([`../restore-reflink-2026-08-14/`](../restore-reflink-2026-08-14/README.md)).
  배수는 **약 7배**로 인용 가능하다
- ✅ **real-JSON 축소 대조 — 08-13 b 라운드에서 돌았고, 아래 예상은 반증됐다**
  ([`../backup-restore-aws-b-2026-08-13/` §3](../backup-restore-aws-b-2026-08-13/README.md)).
  논리 복구는 **바이트가 아니라 행 수가 지배**한다 — 행이 40배 뚱뚱해져도 행당 비용은 9배만
  늘어(바이트당으로는 4.5배 **빨라진다**). 아래 원문은 기록으로 남긴다:
- ~~🔴 **real-JSON 축소 대조 1판이 안 돌았다**~~([#202](https://github.com/Shadowfit/init/issues/202)).
  설계 §9-1 의 확정된 규모는 «더미 1억 행 본 측정
  **+ real-JSON 소규모(~1~2GB) 대조 1판**» 이었는데, rig 에 그 팔이 없다(`backup_sweep.sh` ·
  `run_all.sh` 어디에도 real 무대가 없다). **하필 그 대조가 막으려던 것이 Q2 다** —
  `mysqldump` 는 행을 SELECT 해서 뽑아 off-page 저장을 타므로, 더미 JSON(~115B, 인라인)으로만
  재면 **논리 백업의 불리함이 실제보다 작게 나온다**(인덱스 배수가 더미 660배 ↔ real-JSON
  9,000배로 과소평가됐던 것과 같은 함정). 즉 **§1 의 21분은 real-JSON 에서 더 나빠질 값**이다
- **팔 A 복구는 단일 스레드 재적재**다. MySQL Shell 병렬 덤프(설계 §9-1 ①)는 이번 범위 밖이라
  **「논리 백업의 복구는 21분」이 아니라 「`mysqldump` + `mysql` 재적재는 21분」** 이다
- **binlog 를 별도 볼륨에 두는 구성은 안 봤다.** 지금 구성은 datadir 과 같은 디스크다
- **값 분포 한계** — 무대가 단일 템플릿 복제라 카디널리티가 균일하다. 백업/복구는 전량 처리라
  분포 의존이 적지만, R1 의 0행은 **「구조는 있다」까지만** 말한다
- **`DROP_CACHES=1` 은 판 사이에만** 건다. 판 안에서 생기는 캐시 효과는 그대로다 (§3 이 그 사례)

## 결정 로그

- **2026-08-13: 본 측정 완주.** 게이트 4개 통과 후 EC2 승격, 1억 행 무대에서 9판.
  전 단계 OK, `errors=0`, 복구 검증 전판 일치. 러너가 **스스로 정지**했다 —
  `run_all.sh` 는 최종 S3 업로드가 성공했을 때만 끄므로(`:396`) 자동 정지 자체가 완주 신호다.
- **2026-08-13: 팔 B 복구 시간에 캐시 오염을 발견**(§3). 표가 「빠르다」로 보이는데
  **실제로는 디스크에 안 내려간 시간**이라 이 repo 가 말하는 «조용히 틀리는» 부류다.
  판정 방향은 유지, **배수는 철회.** 이슈 [#201](https://github.com/Shadowfit/init/issues/201).
- **2026-08-13: 從 R1 이 08-08 이후 처음으로 답을 냈다.** 회수도 S3 경로로 성공했다
  (#198·#199 수정분의 첫 실사용).
- **2026-08-14: §3 의 «페이지 캐시» 진단이 틀렸다**([#210](https://github.com/Shadowfit/init/issues/210)).
  #201 이 그 가설로 넣은 `sync` 계측이 **15~22ms** 를 내면서 가설 자체를 반증했다 — 원인은
  **XFS reflink**(복사가 아예 안 일어남)였다. **틀린 가설이 넣은 계측이 틀렸음을 스스로 드러낸**
  구조라, 계측을 값으로 남기기로 한 #201 의 설계 판단이 값을 했다.
- **2026-08-14: 배수 교정.** 「143배」 철회 → **약 7배**. 판정 방향은 유지.
  `backup_sweep.sh` 에 `--reflink=never` 를 박아 다음 라운드가 같은 값을 다시 내지 않게 했다.
