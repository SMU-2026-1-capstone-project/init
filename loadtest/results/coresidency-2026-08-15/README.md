# 동거 용량 — rig (2026-08-15)

설계: [`../../../docs/decisions/ai-coresidency-capacity.md`](../../../docs/decisions/ai-coresidency-capacity.md)
탑승 목록: [`../../AWS-RIDE-ALONG.md`](../../AWS-RIDE-ALONG.md) 主-**P6**

> ⚠️ **이 디렉터리는 아직 rig 뿐이다. 측정값은 없다.**
> 측정 라운드는 관례대로 `coresidency-aws-<측정일>/` 로 따로 남는다

> 🟢 **라운드 조건이 확정됐다 — 남은 것은 실행뿐이다** (2026-08-16 착수 confirm)
>
> | 조건 | 값 | 어디에 |
> |---|---|---|
> | 팔 | **A B C** (D 제외) | `coresidency_sweep.sh:42` |
> | 레벨 | **20 40 60 80 120 160** (6개) | `coresidency_sweep.sh:47` · `../../aws/run_all.sh:106` — **두 곳이 같아야 한다** |
> | 판 | 팔당 버림 1 + 본 3 → 3팔 × 4 = **12판** · 판당 `DUR=90s` | `:48-49` |
> | 從 부하 | **19 req/s 고정** (기본값 없음 — 부르는 쪽이 근거와 함께 준다) | `:65` |
> | 무대 | 대상 **c7i.4xlarge** + 부하기 **c7i.large** (2대) | [러너 README 「P6 — 2대 구성」](../../aws/README.md) |
> | 예상 | 12판 ≈ 2시간, 라운드 전체 ≈ 3.5시간 | 설계 §5-2 |
>
> **선행 결함 2건은 코드로 닫혔다.** [#222](https://github.com/Shadowfit/init/issues/222)(팔 A 불성립)는
> (a)안 — «B 와 같은 구성 + ghz 부하 없음» — 으로 재정의됐고,
> [#223](https://github.com/Shadowfit/init/issues/223)(ghz 미배선)은 요청/초 고정으로 배선돼
> `GHZ_RPS`·`GHZ_DATA`·`GHZ_TOKEN` 이 비면 라운드를 거부한다.
>
> **측정 결과는 여기가 아니라 `coresidency-aws-<측정일>/` 에 남는다:**
> [`../coresidency-aws-2026-08-15/`](../coresidency-aws-2026-08-15/README.md) — **예비 라운드**
> (이 rig 의 **#245 이전** 판으로 돈 것. 완주했고 회수했다)
>
> 🔴 **위 확정 조건으로 돈 라운드는 아직 없다** — #222·#223 수정과 6레벨 격자는 EC2 에서
> 검증된 적이 없다. 그래서 라운드 첫 단계가 축소 리허설이고, **실패하면 정판을 안 돈다**
> (예비 라운드에서 이 게이트가 **세 번** 정판을 막았다).
>
> **(a)안의 대가**: 팔 A 에 MySQL·Spring 이 유휴로 얹히므로, 설계 §3-1 이 산출물로 삼았던
> «A↔156 = 서비스 경로 오버헤드» 가 **«경로 오버헤드 + 유휴 동거 비용»** 이 되어 둘이 안 갈린다.
> 결과 문서에 이 문장을 그대로 넣는다..

---

## 무엇을 재는가

2026-08-14 의 **「동시 156세션」은 AI 컨테이너만 있는 박스** 값이다. 실제 배포는 MySQL·Spring 이
한 호스트에 산다. **동거하면 얼마가 되는지가 미측정**이고, 그 숫자가
「한 대로 되나 = 수평확장이 필요한가」를 가른다.

| 팔 | 구성 | 답하는 것 |
|---|---|---|
| **A** | AI + Spring·MySQL, 캡 없음, **ghz 부하 없음** | 유휴 동거 기준선. 🔴 «AI 단독» 이 아니다 — 2026-08-16 (a)안([#222](https://github.com/Shadowfit/init/issues/222)) |
| **B** | A 와 **같은 구성** + **ghz 부하** | 옆이 «일할 때» 얼마가 되나 · 무엇이 먼저 뺏나 |
| **C** | + CPU 캡 | 캡이 **무엇을 지키는지** ([#212](https://github.com/Shadowfit/init/issues/212)) |
| **D** | + 관측 스택 | 선택. obs 비용을 숫자로 |

---

## 🔴 §2. rig 을 짜다 발견한 것 — 합성 인체로는 rep 이 안 생긴다

설계 §8 은 *"rep 이 완성돼야 `SavePoseDataBatch` 가 나가고 그래야 Spring·MySQL 이 실제로
일한다"* 를 전제로 뒀다. **그 전제를 rig 작성 중에 실측으로 확인했고, 틀렸다.**

`gen_frames.py` 가 합성 스쿼트 1주기 30프레임의 무릎 각도를 `squat_analyzer` 의 실제 계산
경로(`_extract_raw_metrics`)로 잰 결과 (2026-08-15, 로컬 MediaPipe):

```
검출 30/30 (전부 성공)
무릎 각도 86.95° ~ 124.30°
```

rep 상태기계는 **150° 이상(서있음)과 100° 이하(앉음)를 둘 다** 지나야 한 바퀴 돈다
(`squat_analyzer.py:264-265`). **앉음 쪽은 지나는데 서있음 쪽에 25° 이상 모자란다.**
즉 이 프레임으로는 rep 이 영원히 0 이다.

> ⚠️ 초판이 이 자를 `_phase_from_angles` 의 95/155 로 잡았다가 교정했다 — 그쪽은 **국면
> 이름표용**이고 rep 을 세는 것은 100/150 이다. 문턱이 두 벌인 것 자체가
> [#218](https://github.com/Shadowfit/init/issues/218) 이다. (판정은 안 바뀐다: 124.30° 는 150 에도 못 닿는다.)

> 📌 기존 rig 이 단언한 것은 **«검출되는가»** 뿐이었다(`assert_detectable`). 그건 통과한다.
> rep 은 그보다 센 조건인데 **아무도 확인한 적이 없었다.**

### 원인은 기하가 아니라 **z** 다 ([#217](https://github.com/Shadowfit/init/issues/217))

`calculate_angle` 이 3D(x·y·z)로 각도를 내는데, MediaPipe 의 z 는 단안 추정이라 합성
스틱 피겨에서 무너진다. **같은 프레임인데 2D 로는 완벽히 서 있다:**

| 자세 | 3D (현행 코드) | 2D (z 만 뺌) |
|---|---|---|
| 서있음 | **108.57°** | **178.47°** |
| 깊이 앉음 | 79.38° | 148.88° |

→ **`figure()` 의 다리 비율을 고쳐도 소용없다.** 이미 완벽하게 그려져 있다.
→ 그리고 2D 로 바꿔도 안 된다 — 그쪽은 «앉음»(≤100°)에 못 닿는다(최저 148.88°).
   **어느 축으로도 한쪽 문턱을 못 넘는다.** 합성으로 rep 만들기는 여기서 닫힌다.

### 그래서 부하를 두 갈래로 나눈다 (2026-08-15 사용자 결정)

```
부하기 ──프레임 3fps──> AI            (이 디렉터리의 load_ai.py)
ghz   ──SavePoseDataBatch──> Spring → MySQL   (기존 loadtest/ghz/ rig)
```

- 🟢 **rep 에 안 기댄다** → [#196](https://github.com/Shadowfit/init/issues/196)(쓸 수 있는 스쿼트 입력이 없다)·
  [#192](https://github.com/Shadowfit/init/issues/192)(기준 좌표 0행)에 **막히지 않는다.** 둘 다 열려 있는 이슈다
- ✅ **ghz 갈래가 배선됐다** (2026-08-16, [#223](https://github.com/Shadowfit/init/issues/223)).
  **요청/초 고정**(`--rps`)이다 — 부하기 동시성이 아니라 rate 를 묶는다. 세션 수를 흔드는
  실험에서 옆의 부하까지 같이 흔들리면 조작 변수가 둘이 되기 때문이다.
  - 팔 **B·C·D 에만** 걸린다. 팔 A 는 «옆이 유휴» 가 정의다
  - ghz 창이 AI 측정 창을 **앞뒤 `GHZ_PAD`초씩 감싼다** — 측정 구간 내내 부하가 걸려 있게
  - 판 사이 `reset_ghz_rows` 로 從 부하가 쌓은 행을 지운다. 안 지우면 «판 순서» 가
    «세션 수» 와 같은 축에 겹친다
  - 🔴 **`GHZ_RPS` 에 기본값이 없다.** 이 값은 «정한» 값이라, 무엇을 근거로 정했는지가
    결과의 조건 칸에 같이 가야 한다. 그리고 **실측 rps 를 `ghz.tsv` 에 따로 남긴다** —
    목표와 실제가 다르면 그건 다른 조건이다
- 🔴 **대가**: 두 부하의 비율이 «생겨난» 값이 아니라 **«정한» 값**이다. 실사용에서 프레임과
  적재가 어떤 비로 붙는지는 이 rig 이 답하지 않는다. **결과 문서에 이 문장을 그대로 넣는다.**

---

## 파일

| 파일 | 역할 |
|---|---|
| `gen_frames.py` | 합성 스쿼트 → base64 JPEG 배열(`frames.json`). **각도·검출을 먼저 재고 사실을 meta 에 박는다** |
| `synthetic_body.py` | 합성 인체 — `measure_ai_concurrency.py:55-96` 의 **사본**(사유는 파일 docstring) |
| `frames.json` | 생성물. 30프레임 · 408KB · 프레임당 13.6KB |
| `load_ai.py` | 동시 세션 부하기. **세션을 Spring 통해 실물로 연다**(안 그러면 전부 거절) |
| `probe.sh` | 승격 게이트 G0~G3 |
| `coresidency_sweep.sh` | 팔 × ramp × 판 오케스트레이션 + `docker stats` 샘플러 |

---

## 게이트 (`probe.sh`)

| # | 무엇 | 왜 |
|---|---|---|
| **G0** | 합성 인체 사본 ↔ 원본 **글자 단위 대조** | 두 rig 이 다른 인체를 재면서 같은 이름을 쓰는 것을 막는다 |
| **G1** | 프레임 자산 — 전 프레임 검출 | 검출이 깨지면 재는 것이 «추론» 이 아니라 «탐지 실패» 다 |
| **G2** | 세션 개설 → 프레임 수락 | 🔴 **급소.** 세션이 안 열리면 전 프레임이 거절되는데 **요청 수·지연은 정상으로 찍힌다** |
| **G3** | 컨테이너 자원 한도 | 팔이 실제로 갈리는지. 메모리 캡이 없으면 첫 세션에서 죽는다([#214](https://github.com/Shadowfit/init/issues/214)) |

---

## 읽는 법 — 세 결과를 뭉치지 않는다

`load_ai.py` 는 응답을 세 갈래로 분류한다. **뭉치면 용량 신호와 품질 신호가 섞인다.**

| 분류 | 뜻 | 축 |
|---|---|---|
| `ok` | 검출 성공 | — |
| `nolease` | 풀에 자리가 없다 (`분석기가 없습니다`) | **용량** |
| `nopose` | 검출이 깨졌다 (`감지할 수 없습니다`) | **품질** — [#164](https://github.com/Shadowfit/init/issues/164) 가 30%→95.6% 로 고친 그 지표 |

### 판 순서 — 라틴 방격 (2026-08-16)

라운드마다 팔 순서를 한 칸씩 돌린다. 팔이 3개·`REPEATS=3` 이면 각 팔이 판 위치 1·2·3 에
한 번씩 선다(설계 §5):

```
r1: A B C     r2: B C A     r3: C A B
```

🔴 **초판은 팔을 블록으로 돌았다**(A 13판 → B 13판 → C 13판). 그러면 팔 C 가 항상 라운드의
마지막이라 «캡 효과» 와 «시간이 흐르며 생긴 것»(EBS 버스트 크레딧·테이블 성장·열)이 같은
축에 겹쳐 **원리적으로 안 갈린다.** 4차 풀 사이징 라운드가 «N 클수록 +32%» 를 냈다가 순서를
뒤집으니 «작을수록 −36%» 로 부호가 바뀐 것이 이 오염이었다. 버림판은 그 팔이 **처음 나오는
라운드에서만** 돈다(팔당 1회). `REPEATS` 가 팔 수의 배수가 아니면 스윕이 «부분 균형» 이라고
경고한다 — 그때는 순서 효과가 남으므로 결과에 적는다.

### 시간축 — `stats` 와 요청 표를 잇는다 (2026-08-16)

`coresidency.tsv` 의 **`warm_lo`·`warm_hi`** 가 그 판의 정상 상태 구간을 **epoch** 로 적는다.
`stats_*.tsv`(epoch 로 찍힌다)를 이 구간으로 잘라야 **Q2(무엇이 먼저 뺏는가)** 에 답할 수 있다 —
판 전체 평균으로 뭉개면 워밍업·램프 구간이 섞인다. 從 R5([#229](https://github.com/Shadowfit/init/issues/229))의
«N=0 기동 직후 RSS» 도 같은 축으로 집는다(측정창 시작 **이전** 표본이 그 구간이다).
요청 표 `req_*.tsv` 에도 `t_abs` 열이 **끝에** 붙는다.

---

## 알려진 한계 (미리 박제)

- 🔴 **합성 인체다.** 절대 성능값을 실사용으로 인용하지 말 것. 실사진은 행 크기·JPEG 특성이 다르다
- 🔴 **두 부하의 비율은 정한 값이다** (위 §2)
- 🔴 **fps 는 안 흔든다.** AI 에 유입 속도 상한(프레임 간격 300ms)이 있어 3fps 위로 올리면
  재는 것이 «용량» 이 아니라 «상한에 걸린 비율» 이 된다. 흔드는 것은 **세션 수** 하나다
- ⬜ **`docker stats` 폴링 자체가 대상을 흔든다.** 간격(`STATS_SEC`)을 조건에 기록할 것
- ⬜ **세션 상태는 AI 프로세스 메모리에 있다**(`session_state.py:243`). 판 사이 재기동이
  필수이고, 그 재기동 비용이 라운드 시간에 들어간다

---

## 축소 리허설 (무인 실행 전 필수)

```bash
python gen_frames.py --out frames.json          # ai-server 이미지 안에서
BASE=... AI=... TOKEN=... bash probe.sh          # 게이트
HOST=... TOKEN=... LEVELS="5 10" DUR=20 REPEATS=1 bash coresidency_sweep.sh
```

**리허설 수치는 측정값이 아니다.** 경로만 본다.
