# 풀 사이징 cliff × 동시성 — 실측 결과 (2026-08-08)

설계: [`../../../docs/decisions/pool-cliff-vs-concurrency.md`](../../../docs/decisions/pool-cliff-vs-concurrency.md)
선행: [`../../../docs/decisions/pose-ingest-downsampling.md`](../../../docs/decisions/pose-ingest-downsampling.md) §5-1(7)(8)

> 🔴 **이 디렉터리가 존재하는 이유**: 지난번(2026-07-25) 분리 배포 실측은 결과 JSON 을 로컬
> 세션에만 두고 인프라와 함께 지웠다. 그래서 이번에 같은 질문을 다시 물으려면 **처음부터 다시
> 돈을 내야 했다.** 같은 일을 반복하지 않으려고 원시 파일을 전부 커밋한다.

---

## 0. 한 줄 요약

**절벽은 없었다 — pool 5~20 · c 10~100 어디에서도.** 그리고 그 이유가 이 실험의 진짜 산출물이다:
**다운샘플(R=5)이 병목을 DB 쓰기에서 백엔드 CPU 로 옮겨놨다.**

그 결과 `maximum-pool-size: 15` 라는 현재 값은 **틀리지 않았지만 근거가 낡았다.** 그 값을 정당화한
실험(§5-1(8))은 **배치당 25행을 쓰던 코드**에서 나왔고, 지금 코드는 5행을 쓴다.

---

## 1. 결과 — c 별 cliff 위치

판정: 각 c 에서 `pool=20` 의 RPS 를 plateau 로 잡고 **그 90% 미만이면 cliff 아래**(설계 §2 확정).
⚠️ 90% 는 근거 있는 값이 아니라 **사후에 유리한 기준을 고르지 않으려고 미리 못박은 선**이다.

| c | plateau(pool=20) | pool=5 | plateau 대비 | cliff |
|---|---|---|---|:--:|
| 10 | 205.2 | 209.6 | 102% | **없음** |
| 20 | 201.3 | 212.6 | 106% | **없음** |
| 30 | 210.0 | 203.2 | 97% | **없음** |
| 50 | 206.4 | 209.6 | 102% | **없음** |
| 100 | 203.9 | 208.9 | 102% | **없음** |

**풀을 4배(20→5) 줄여도 처리량이 안 변한다.** 전 구간 실패 0.

전체 원시 수치는 [`sweep2.tsv`](./sweep2.tsv), ghz 원본은 [`ghz/`](./ghz/).

---

## 2. 왜 절벽이 없나 — 지표가 답한다

`hikaricp_*` 원시 시계열([`metrics/`](./metrics/), 그래프는 이 폴더의 `*.svg`):

| 지표 | 최대 | p90 | 중앙 |
|---|---|---|---|
| `connections_pending` | **96** | 5 | 0 |
| `connections_active` | **20** (= 상한) | 5 | 0 |
| `connections_acquire_seconds_max` | **7.28초** | 3.64 | 0.006 |
| `connections_timeout_total` | **0** | 0 | 0 |
| `system_cpu_usage` (app) | **1.00** | 0.76 | 0.015 |

**풀은 포화됐다.** active 가 상한에 붙고 pending 이 96 까지 갔다 — c=100 의 거의 전부가 줄을 섰다.
획득 대기도 7.28초까지 갔다.

**그런데 무너지지 않았다.** 타임아웃이 0 이다. 커넥션 1건당 작업이 짧아서(5행 INSERT) 대기 7초는
30초 한도에 못 미치고 큐가 빠진다. 즉 **포화 ≠ 붕괴**다.

그리고 처리량이 풀 크기와 무관한 이유는 따로 있다 — 부하 중 컨테이너 CPU:

```
백엔드 컨테이너   p90 128%   최대 155%   (2 vCPU = 200% 만점)
MySQL 컨테이너    p90  53%   최대  57%
```

**MySQL 은 놀고 백엔드가 CPU 를 태운다.** 풀을 늘려도 줄여도 처리량이 ~200 RPS 에 고정되는 건
병목이 커넥션이 아니라 백엔드 CPU(gRPC 디코드 + 25프레임 JSON 파싱 + 대표프레임 선택)이기 때문이다.

---

## 3. 대조군 — 다운샘플이 원인이라는 직접 증거

`DOWNSAMPLE_WINDOW` 만 5 → 1 로 되돌려(= 25행 저장, 옛 코드와 같은 쓰기량) 다시 빌드해 비교했다.
**같은 인프라·같은 부하·같은 풀·둘 다 cold 조건.**

| c=100, pool=10 | RPS | p50 | p95 | p99 | 실패 |
|---|---|---|---|---|---|
| R=5 (현재 코드) | **168.7** | 528ms | 1,024 | **1,381** | 0 |
| R=1 (옛 코드 상당) | **98.3** | 669ms | 2,093 | **6,740** | 0 |

**RPS 1.7배, p99 4.9배.** 지난번 47% 붕괴가 이번에 재현되지 않은 이유는
**측정이 틀려서가 아니라 코드가 바뀌어서**다.

> ⚠️ 이 대조군은 **cold vs cold** 비교라 유효하지만, `pool=20` 짝(120.5)은 warm 실행과 섞여
> 비교 가능하지 않다(§5 오류 ②). 그래서 위 표에 넣지 않았다.

---

## 4. 환경

| | |
|---|---|
| 리전 / 구성 | `ap-northeast-2`, EC2 3대 (**측정 후 전부 삭제**) |
| DB | `t4g.medium` (2vCPU/4GB), self-managed MySQL 8.0, 버퍼풀 768M, 컨테이너 `--memory 3g` |
| App | `t4g.medium`, 백엔드 컨테이너 `--memory 2g` |
| **obs** | `t4g.small` — Prometheus + Grafana. **이번에 추가** |
| 부하기 | ghz v0.120.0. **v2 는 obs 인스턴스에서** (v1 은 app 에서 — §5 오류 ①) |
| 데이터 | `pose_data` **3,750,000 행** (디스크 25G/50G) |
| warmup | c=3, 30초 — 모든 테스트 pool 보다 작게 |
| 각 판 | `-n 2000` 고정 |

**3대 구성의 효과**: obs 를 분리하니 수치와 그래프를 **같은 판에서** 얻었다. 2코어 로컬에서
"수치냐 그래프냐" 택1이던 제약이 사라졌다.

---

## 5. 🔴 이번 실험에서 틀린 것 — 셋 다 실행해서 알았다

### ① 부하기를 백엔드와 같은 박스에 뒀다 (설계 누락)

v1 스윕은 ghz 를 app 인스턴스에서 돌렸다. 설계 문서는 obs 를 분리하는 이유는 적었지만
**부하기 위치는 지난번 구성을 검토 없이 이어받았다.** 부하기가 백엔드와 2 vCPU 를 나눠 쓰면
처리량 천장이 낮아진다. → v2 를 부하기 분리로 재실행.

**결과적으로 v1 과 v2 의 결론은 같았다**(v1 196~205 / v2 201~213 RPS, 둘 다 cliff 없음).
v2 는 낭비가 아니라 **독립 재확인**이 됐지만, 재실행을 결정한 근거는 아래 ②로 틀린 것이었다.

### ② cold JVM 을 풀 효과로 오독했다 — 이 프로젝트가 이미 겪은 함정

단발로 잰 `pool=20 → 218.5` 와 `pool=10 → 168.7` 을 풀 차이로 읽고 *"절벽이 있다"* 고 판단했다.
그런데 **218.5 는 스윕 내내 떠 있던 warm 백엔드**, **168.7 은 방금 재시작한 cold 백엔드**였다.
워밍업 없이 비교한 것이다.

[`load-test-strategy.md`](../../../docs/decisions/load-test-strategy.md) 가 이미 적어둔 교훈:
*"cold JVM 은 초반 인터프리터 모드로 2~5배 느려 측정을 오염시킨다 — JVM 부하 측정은 워밍업
통제가 필수."* 같은 함정에 그대로 빠졌다.

### ③ ②를 근거로 "v1 스윕 10판 전부 못 쓴다"고 선언했다

틀린 비교 하나로 멀쩡한 데이터셋을 폐기 판정했다. **잘못된 근거에서 나온 옳은 행동**(재실행)이라
결과는 다행이었지만, 판단 자체는 틀렸다.

> 📌 남는 것: **"차이가 보였다"와 "차이가 있다"는 다르다.** 조건을 한 번에 하나만 바꿨는지
> 먼저 확인해야 했다.

---

## 6. 이 실험이 답하지 못하는 것

- **절대 RPS 는 참고용.** 프로덕션 RDS 가 아니라 self-managed MySQL on EC2 이고 페이로드도 합성이다
- **템플릿이 원본과 다르다.** 세션 601 원본이 소실돼 합성 재생성했고(`../../seed/gen_pose_template.py`),
  `joint_coordinates` 가 원본(~2.3KB)보다 약 10% 작다
- **pool < 5 는 안 봤다.** 하한을 5(이론 공식값)로 잡았다
- **c > 100 은 안 봤다.** DAU 1,000 가정에서 현실성이 낮다
- **cliff 가 «왜» 그 자리인지**는 여전히 안 나온다 — 이번엔 아예 자리가 없었다
- ⚠️ **c 를 사용자 수로 번역할 수 없다.** rep 간격 실측이 없어 "동시 세션 N = c 얼마" 환산 근거가 없다

---

## 7. 파일

| 경로 | 내용 |
|---|---|
| `sweep2.tsv` | **v2 본 결과** (부하기 분리, 워밍업 통제) |
| `sweep1-shared-loader.tsv` | v1 결과 (부하기 동거). 결론 동일 — 대조용으로 보존 |
| `ghz/` | ghz 원본 JSON 전부 (스윕 각 판 + 대조군 + 단발) |
| `metrics/m-*.json` | Prometheus `query_range` 원본 |
| `*.svg` | 위 JSON 으로 그린 그래프 |
| `plot.py` | JSON → SVG (의존성 없음). 언제든 다시 그릴 수 있다 |
| `sweep.sh` | v2 실행 스크립트 |
