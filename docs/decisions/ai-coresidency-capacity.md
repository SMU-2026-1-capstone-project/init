# 동거 용량 — 「156세션」은 혼자 살 때의 값이다

작성일: 2026-08-15
상태: **설계·rig 완료 (2026-08-15) · 측정 라운드 미실시** — 착수·채택은 사용자 confirm 후 박제.
      rig: [`../../loadtest/results/coresidency-2026-08-15/`](../../loadtest/results/coresidency-2026-08-15/README.md) · 측정값은 `coresidency-aws-<측정일>/` 로 따로 남는다
대상: 「한 대로 되나」를 사실로 만드는 것. **수평확장 결정의 선행 조건**이지 수평확장 설계가 아니다.
연관: [`./load-test-strategy.md`](./load-test-strategy.md) §1, [`../19-deployment.md`](../19-deployment.md) §7,
[`../../loadtest/results/ai-concurrency-aws-2026-08-14/`](../../loadtest/results/ai-concurrency-aws-2026-08-14/README.md),
[#212](https://github.com/Shadowfit/init/issues/212) · [#214](https://github.com/Shadowfit/init/issues/214),
[[project_loadtest_env_constraint]] · [[feedback_measure_design_needs_repeats]]

---

## 0. 이 문서가 여는 것

2026-08-14 에 **물리 8코어에서 동시 156세션**을 실측했다. 가정 피크 67 의 2.3배라
「용량은 해결됐다」로 읽히기 쉽다. 그런데 그 값에는 조건이 하나 붙어 있다 —
**AI 컨테이너만 있는 박스**에서 난 값이다.

실제 배포는 그렇지 않다. `docker-compose.prod.yml` 은 **MySQL·Spring·AI 를 한 호스트**에
올린다(관측 스택은 profile 로 분리). 그러면 156 은 내려간다. **얼마나 내려가는지는 안 쟀다.**

> **그래서 지금은 「수평확장이 필요한가」에 답할 수 없다.**
> 동거 값이 120 이면 100 동접은 한 대로 끝나고 수평은 «성능» 이 아니라 **«가용성» 결정**이 된다.
> 40 이면 반대다. **같은 질문에 정반대 답이 나오는데 그걸 가르는 숫자가 없다.**

이 문서는 그 숫자 하나를 만든다.

---

## 1. 측정으로 답할 질문

| # | 질문 | 왜 이게 질문인가 |
|---|---|---|
| Q1 | **동거하면 동시 세션이 얼마가 되나** | 156 은 단독 값이다. 이 프로젝트의 모든 용량 서술이 그 위에 서 있다 |
| Q2 | **무엇이 먼저 뺏는가** — MySQL·Spring·AI 중 | 「내려갔다」만으로는 못 고친다. 뺏는 쪽을 알아야 캡을 어디에 거는지가 정해진다 |
| Q3 | **캡을 걸면 회복되나** ([#212](https://github.com/Shadowfit/init/issues/212)) | 캡의 목적은 «AI 를 보호» 가 아니라 «서로 안 뺏기» 다. 실제로 그 효과가 있는지는 미검증 |
| Q4 | **품질도 같이 내려가나** | 처리량이 아니라 **검출률**이다. #164 가 30% → 95.6% 로 고친 그 지표가 CPU 경합에서 다시 무너지는지 |
| Q5 | 관측 스택을 같이 올리면 얼마를 더 먹나 | 2026-08-08 에 obs 를 분리한 이유가 이것인데, **비용을 숫자로 적은 적이 없다** |

**Q2 가 급소다.** Q1 은 한 숫자지만 Q2 는 다음 작업을 정한다.

---

## 2. 🔴 기존 rig 으로는 못 잰다 — 새로 만들어야 하는 이유

[`loadtest/measure_ai_concurrency.py`](../../loadtest/measure_ai_concurrency.py) 가 이미 있고
08-14 의 156 을 낸 물건이다. 그런데 **이 실험에는 쓸 수 없다.**

```python
from app.core.mediapipe_detector import get_detector   # rig:50
```

**검출기를 프로세스 안에서 직접 부른다.** 컨테이너도, FastAPI 도, gRPC 도 안 거친다.
그건 의도였다 — 「프레임당 추론 비용」은 환경에 안 묶이는 양이라 그렇게 재야 옮겨 쓸 수 있다.

하지만 **동거는 정확히 «환경» 에 관한 질문**이다. 컨테이너 경계·cgroup·네트워크 스택이
빠지면 잴 대상 자체가 없다. 그래서 이 실험의 부하는 **실제 서비스 경로**여야 한다:

```
부하기 ──POST /api/v1/pose (8000, AI_PUBLIC_TOKEN)──> AI 컨테이너
                                                        │ rep 완성 시
                                                        ↓ gRPC SavePoseDataBatch
                                                     Spring ──> MySQL
```

프론트 → AI HTTP 직결(분기 H2)이 실제 프레임 경로이므로 **그 경로에 건다.**

> 🔴 **초판은 여기에 «Spring·MySQL 부하는 AI 부하의 결과로 자연히 따라온다» 고 적었다.
> rig 을 짜면서 실측으로 반증됐다** (2026-08-15, [rig README §2](../../loadtest/results/coresidency-2026-08-15/README.md)).
> 합성 인체의 무릎 각도가 **86.95° ~ 124.30°** 라 rep 상태기계의 «서있음»(≥155°) 문턱에
> 30° 이상 모자란다 — **rep 이 영원히 0 이고, 따라서 `SavePoseDataBatch` 가 한 번도 안 나간다.**
> 검출은 30/30 으로 멀쩡하다. 기존 rig 이 단언한 것이 «검출» 뿐이라 아무도 몰랐다.
>
> → **부하를 두 갈래로 나눈다**(사용자 결정): AI 는 프레임으로, Spring·MySQL 은 **기존 ghz
> `SavePoseDataBatch` rig** 으로 **동시에** 건다. rep 에 안 기대므로
> [#196](https://github.com/Shadowfit/init/issues/196)·[#192](https://github.com/Shadowfit/init/issues/192)
> (둘 다 열린 이슈)에 막히지 않는다.
> **대가**: 두 부하의 비율이 «생겨난» 값이 아니라 **«정한» 값**이다. 실사용의 프레임↔적재 비는
> 이 실험이 답하지 않는다 — 결과 문서에 이 문장을 그대로 넣는다.

> ⚠️ **그래서 이 rig 은 `measure_ai_concurrency.py` 를 대체하지 않는다.** 저쪽은 «프레임당 비용»,
> 이쪽은 «동거 상태의 서비스 용량» 이다. 두 값을 같은 표에 올리지 않는다.

---

## 3. 팔 설계

무대는 **한 박스**다. 팔은 그 박스에 무엇이 같이 사는가로 갈린다.

| 팔 | 구성 | 성격 |
|---|---|---|
| **A** | AI 단독 | **기준선.** 08-14 의 156 을 «이 rig 으로» 재현한다 — rig 이 바뀌었으므로 값도 다시 세운다 |
| **B** | AI + Spring + MySQL, **캡 없음** | 현행 `docker-compose.prod.yml` 그대로. Q1·Q2 |
| **C** | AI + Spring + MySQL, **캡 있음** | [#212](https://github.com/Shadowfit/init/issues/212)·[#214](https://github.com/Shadowfit/init/issues/214) 적용. Q3 |
| **D** | C + 관측 스택(`--profile obs`) | Q5. **선택** — 라운드가 길어지면 뺀다 |

### 3-1. 팔 A 가 왜 필요한가 — 「재현」이 아니라 「환산」이다

08-14 의 156 은 **다른 rig**(in-process)으로 잰 값이다. 새 rig 은 HTTP·base64 디코딩·
컨테이너 경계를 포함하므로 **같은 박스에서도 다른 숫자가 나온다.** 팔 A 가 없으면
B 와 156 을 비교하게 되고, 그러면 **「동거 때문에 내려간 몫」과 「rig 이 달라서 내려간 몫」이
분리되지 않는다.**

📌 그리고 A ↔ 156 의 차이 자체가 산출물이다 — **서비스 경로의 오버헤드**가 그 값이다.

### 3-2. 팔 C 가 #212 의 값을 만들어 준다

[#212](https://github.com/Shadowfit/init/issues/212) 는 「관측 스택·MySQL 에 얼마를 줄지」의
근거가 없어 열려 있다. 팔 B↔C 대조가 **캡이 실제로 무엇을 지키는지**를 보여주면
그 값이 판단이 아니라 측정 위에 선다. **이 실험을 하는 두 번째 이유다.**

---

## 4. 지표 — 걷는 것과 보는 것

| 축 | 지표 | 출처 |
|---|---|---|
| **용량** | 포화 시 초당 처리 프레임 → ÷ 3fps = 동시 세션 | 부하기 |
| **지연** | 프레임 응답 p50·p99 | 부하기 |
| **품질** 🔴 | **검출률**(랜드마크가 실제로 나온 프레임 비율) | 응답 본문 |
| 귀속 | 컨테이너별 CPU·메모리 (cgroup) | `docker stats` 샘플러 |
| 귀속 | MySQL `threads_running`·`innodb_os_log_fsyncs` | mysqld_exporter |
| 귀속 | Spring gRPC 처리량·p99, HikariCP `pending` | `/actuator/prometheus` |
| **從** | **AI 컨테이너 RSS 실사용 ↔ 유도식 차이** ([#229](https://github.com/Shadowfit/init/issues/229)) | 같은 `docker stats` 샘플러 |

### 4-1. 從 — 메모리 유도식의 «공식 밖 몫» ([#229](https://github.com/Shadowfit/init/issues/229))

**이 라운드에 얹으면 추가 비용이 거의 0 이다.** 이미 동시 세션을 ramp 하며 `docker stats` 로
컨테이너별 메모리를 걷는다 — 필요한 것은 **걷은 값을 유도식과 대조하는 것뿐**이다.

검출기 풀 상한 공식이 **검출기만** 센다(`memory_ceiling()`):

```
상한 = (컨테이너 한도 − 기본 RSS 100.5MB) ÷ 검출기 98.7MB
```

그리고 코드 자신이 *"프레임 버퍼·base64 임시·파이썬 힙·gRPC 는 **미측정**이라 안 뺐다"* 고
경고한다. 그래서 공식에 **딱 맞추면 풀이 세션을 거절하기 전에 컨테이너가 OOM 으로 죽고**,
이 프로젝트가 전제로 삼는 비대칭 실패(«덜 받아짐»=안전)가 **«죽음»=위험으로 뒤집힌다.**

| 세션 수 N 에서 볼 것 | 무엇을 답하나 |
|---|---|
| N=0(기동 직후) RSS | 기본 RSS **100.5MB** 가 지금도 맞는가 (M2 실측값, x86 미검증) |
| `RSS − (100.5 + 98.7×N)` | **공식 밖 몫.** 이게 «여유분» 의 근거가 된다 |
| 그 몫이 N 에 비례하는가 | 비례하면 «세션당 상수», 아니면 «고정 오버헤드» 로 공식에 넣는다 |

🔴 **지금은 숫자를 못 적는다.** 여유를 «몇 %» 로 할지 근거가 없어 `docker-compose.prod.yml`·
`.env.example`·`19-deployment.md` 는 **경고만** 달아뒀다([[feedback_no_arbitrary_threshold_values]]).
이 從 이 그 빈칸을 채운다.

⚠️ **팔 A~D 전부에서 걷는다.** 캡이 걸린 팔(C·D)에서는 «한도에 얼마나 붙었나» 가 같이 보이고,
그게 [#212](https://github.com/Shadowfit/init/issues/212) 의 CPU 캡 값과 **같은 라운드에서**
답해지는 자리다.

🔴 **검출률을 처리량과 같은 급으로 둔다.** [#164](https://github.com/Shadowfit/init/issues/164) 가
검출률 30% → 95.6% 로 고친 전례가 있고([`pool-verify-2026-08-11`](../../loadtest/results/pool-verify-2026-08-11/README.md)),
그건 **처리량 지표에 안 잡히는 고장**이었다. CPU 를 뺏겨 프레임이 밀리면 같은 종류의 붕괴가
다시 날 수 있다 — **처리량만 보면 「느려졌네」로 읽고 넘어간다.**

---

## 5. 판 설계

- **버림판 팔당 1** — 첫 판은 컨테이너 워밍업·버퍼풀·JIT 를 가장 크게 탄다
- 본판 **팔당 3판**, 위치 합을 맞춘 배열(각 팔이 판 순서 1·2·3 에 한 번씩)
- 판 사이 **같은 초기 상태** — 스택 재기동 + `pose_data` 정리
- 판당 부하는 **동시 세션 ramp**(예: 20 → 40 → 80 → 160)로 포화점을 찾는다. 고정 부하로는
  「천장」이 안 나온다

📌 팔이 4개(A~D)면 12판 + 버림 4판이다. **D 를 빼면 9판 + 버림 3판**으로 준다.

---

## 6. 가설과 반증 조건

| # | 가설 | **무엇이 나오면 틀린 것인가** |
|---|---|---|
| H1 | 동거하면 동시 세션이 **내려간다** | B 가 A 와 사실상 같다 (= 동거 비용이 무시할 수준) |
| H2 | 뺏는 주체는 **AI 자신**이다 — 코어를 주는 대로 먹으므로 | 포화 시 AI cgroup CPU 가 지배적이지 않다 |
| H3 | 캡을 걸면 **총 용량은 줄지만 다른 서비스가 산다** | C 에서 Spring p99·MySQL 지표가 B 와 차이 없다 (= 캡이 아무것도 안 지킴) |
| H4 | 검출률은 **처리량보다 먼저** 무너진다 | 포화까지 검출률이 평탄하다 |
| H5 | 관측 스택 비용은 **한 자릿수 %** | D 가 C 대비 두 자릿수 % 내려간다 |

**H3 이 반증되면 [#212](https://github.com/Shadowfit/init/issues/212) 의 전제가 흔들린다** —
캡이 지키는 게 없다면 캡 값을 정하는 일 자체가 다른 문제가 된다. 그것도 산출물이다.

> 임계값은 적지 않는다. 「동시 N세션 이상이면 합격」 같은 기준은 지금 근거가 없다
> ([[feedback_no_arbitrary_threshold_values]]). 이번 범위는 **값을 관측해 적는 것**까지다.

---

## 7. 로컬인가 EC2인가

🔴 **로컬로는 Q1 을 못 답한다.** 개발 장비는 i3-6100 2물리코어에 MySQL+백엔드가 이미
동거 중이라([[project_loadtest_env_constraint]]) **동거 실험의 대조군인 「단독」을 만들 수 없다.**
팔 A 가 성립하지 않으면 §3-1 의 분리가 통째로 무너진다.

그리고 이 실험이 답해야 하는 것은 **「c7i.4xlarge 급에서 156 이 얼마가 되나」** 다. 그 박스가
아니면 옮겨 쓸 수 없다.

→ **EC2 1대.** 08-14 와 같은 타입(c7i.4xlarge)이어야 156 과 비교가 성립한다.
부하기를 같은 박스에 두면 그것이 또 경합 변수가 되므로 **부하기는 별도 소형 인스턴스**다.

---

## 8. 알려진 함정 체크리스트 (착수 전)

- [ ] 🔴 **`AI_MEM_LIMIT` 없이 뜨면 첫 세션에서 죽는다** ([#214](https://github.com/Shadowfit/init/issues/214)).
      팔 B(캡 없음)를 「메모리 캡도 없음」으로 만들면 **팔이 아예 안 돈다.**
      → **팔 B 는 «CPU 캡 없음 + 메모리 캡 있음»** 으로 정의한다(현행 dev compose 와 같은 상태).
      이 한 줄을 안 적으면 무인 라운드가 통째로 날아간다
- [ ] **`AI_PUBLIC_TOKEN`** — 프레임 경로가 `InternalAuthMiddleware` 로 보호된다. 부하기가 이 토큰을 실어야 한다
- [ ] **합성 인체 이미지** — 빈 프레임을 넣으면 재는 것이 「추론」이 아니라 「탐지 실패」가 된다.
      `measure_ai_concurrency.py` 의 생성기를 재사용하고, **매 판 검출 성공을 단언**한다
- [ ] **세션이 실재해야 한다** — 프레임에 `session_id` 가 붙고 검출기 풀이 세션 단위로 점유한다.
      부하기가 세션을 실제로 열고 닫아야 하며, 안 그러면 **풀이 안 돌아 한도에 걸린다**
- [x] ~~**Spring·MySQL 이 실제로 일해야 한다** — rep 이 완성돼야…~~ → 🔴 **이 경로는 막혀 있다**
      (2026-08-15 실측). 합성 인체로는 rep 이 안 생긴다(§2 인용 블록). **ghz 로 따로 건다.**
      대신 새 함정이 생겼다 — **두 부하가 실제로 «동시에» 돌고 있는지**를 판마다 확인해야 한다.
      한쪽이 조용히 죽으면 «동거» 가 아니라 단독인데 표는 정상으로 보인다
- [ ] **`docker stats` 샘플러의 비용** — 관측이 관측 대상을 흔든다. 폴링 간격을 조건에 기록

---

## 9. 의식적으로 안 할 것

| 안 함 | 이유 |
|---|---|
| 수평확장 rig(N대 + 어피니티) | 이 문서는 **한 대의 값**을 만든다. 그 값 없이 N대를 재면 순서가 거꾸로다 |
| 캡 값 튜닝 스윕 | 「몇 %가 최적인가」는 배포 형태가 정해진 뒤. 여기선 **캡 유무**만 |
| 프레임 레이트 스윕 | 3fps 고정. 바꾸면 팔이 배로 는다 |
| 절대 RPS 를 다른 라운드와 비교 | rig·무대가 다르다. 비교는 **같은 라운드 안에서만** |

---

## 10. 미결정 (사용자 confirm 필요)

- [ ] **착수 여부** — EC2 1~2대 · 러너 시간 미상(설계상 12판 + 버림 4판)
- [ ] **팔 D(관측 스택)를 넣을지** — Q5 는 값이 있지만 판이 25% 는다
- [ ] **인스턴스 타입** — c7i.4xlarge 고정(156 과 비교하려면)이 맞는가, 아니면 실제 배포 후보 크기로 갈 것인가
- [ ] **부하기를 어디에 둘지** — 별도 소형 인스턴스(권장) vs 같은 박스(싸지만 경합 변수)
- [ ] **ramp 레벨** — 20/40/80/160 이 맞는가. A 의 천장을 모르면 레벨이 빗나간다
- [ ] **rig 을 새로 쓸지, `chain_*` 을 확장할지** — [`scripts/chain_bootstrap.sh`](../../scripts/chain_bootstrap.sh) 가
      이미 «3서비스가 뜬 EC2» 를 만든다. 부하기만 얹으면 되는지 확인 필요

---

## 결정 로그

- 2026-08-15: 설계 초안. 「동접 100 이면 수평확장인가」 논의에서 출발했는데, **답할 수 없는
  질문**임이 드러나 그 앞의 빈 칸을 문서로 세웠다 — 156 은 단독 값이고 동거 값이 없다.
  - **기존 rig 을 못 쓴다는 것이 이 설계의 첫 발견**이다(§2). `measure_ai_concurrency.py` 는
    검출기를 in-process 로 부르므로 컨테이너·동거를 통째로 못 본다. 「rig 이 있으니 돌리면 된다」로
    시작했으면 **다른 것을 재고 동거 값이라 불렀을 것**이다.
  - 팔 A(단독 재현)를 넣은 이유도 같다 — 없으면 «동거 탓» 과 «rig 탓» 이 안 갈린다(§3-1).
  - [#214](https://github.com/Shadowfit/init/issues/214) 가 팔 B 의 정의를 바꿨다(§8 첫 항목).
    «캡 없음» 을 곧이곧대로 만들면 팔이 안 돈다.
  - **실행 미착수**, §10 미결정 6건.

- **2026-08-15: rig 작성 — 그리고 설계 전제 하나가 실측으로 반증됐다.**
  rig: [`../../loadtest/results/coresidency-2026-08-15/`](../../loadtest/results/coresidency-2026-08-15/README.md)
  (`gen_frames.py` · `synthetic_body.py` · `load_ai.py` · `probe.sh` · `coresidency_sweep.sh`).
  - 🔴 **«rep 이 완성돼 Spring·MySQL 이 따라온다» 가 틀렸다.** 합성 인체의 무릎 각도가
    **86.95° ~ 124.30°** 로 «서있음»(≥155°) 문턱에 30° 이상 모자란다. **rep 이 영원히 0** 이다.
    검출은 **30/30** 이라 기존 rig 의 단언(`assert_detectable`)은 통과한다 — **«검출» 과 «rep» 은
    다른 조건인데 후자를 아무도 확인한 적이 없었다.**
  - 이 발견은 **rig 을 짜지 않았으면 EC2 위에서 났을 것**이다. 그랬으면 «동거를 쟀다» 는 표에
    Spring·MySQL 이 논 판이 담겼을 것이고, #196 이 남긴 함정(HTTP 200 인데 전 필드 0)과 같은 형태다.
  - → **부하를 두 갈래로 분리**(사용자 결정, §2). rep 에 안 기대므로 #196·#192 에 안 막힌다.
  - ⭐ **게이트가 자기 자신을 한 번 잡았다.** `gen_frames.py` 초판이 «검출 실패» 와 «각도 계산
    실패» 를 같은 `None` 으로 뭉개 *「합성 인체는 검출이 아예 안 된다」* 는 오진을 냈다.
    사유를 갈라 적게 고치니 실제로는 검출 30/30 이었다. 이 rig 이 막으려는 것이 정확히 그런
    종류라 rig 자신이 그러면 안 된다 — 그 교훈을 함수 docstring 에 박았다.
  - **G0(사본 드리프트 대조)를 새로 넣었다.** 합성 인체를 사본으로 둘 수밖에 없었는데
    (원본은 import 만 해도 측정이 돈다), 사본은 갈라진다. 원본에서 같은 구간을 떼어 **글자
    단위로 대조**하고 다르면 게이트에서 멈춘다 — 로컬에서 실제로 통과시켜 봤다.
