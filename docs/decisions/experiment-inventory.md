# 실험 재고 — Spring · MySQL · AI 세 파트에서 남은 측정

작성일: 2026-08-17
갱신: 2026-08-20 — **#204 EXPLAIN 스윕 회수 반영**(읽기축 첫 실측 · #205 의 막힌 전제 둘도 같이 풀렸다)
⚠️ 헤더가 08-17 인 채로 08-18 P5 회수분이 본문에 들어가 있었다 — 이번에 같이 맞췄다
상태: **살아 있는 목록.** 실험이 닫히면 여기서 지우지 말고 «완료» 로 표시하고 결과를 건다
연관: [`../../loadtest/AWS-RIDE-ALONG.md`](../../loadtest/AWS-RIDE-ALONG.md)(EC2 탑승 목록) ·
[`./db-portfolio-roadmap.md`](./db-portfolio-roadmap.md)(무엇을 왜 만드나) ·
[GitHub 이슈](https://github.com/Shadowfit/init/issues)(개별 결함)

---

## 0. 이 문서가 있는 이유

「다음에 뭘 재야 하나」의 답이 **이슈 40여 건 · 탑승 목록 · 설계 문서 다섯 곳**에 흩어져 있다.
물어볼 때마다 다시 모으고 있었고, 그러다 **이미 등록된 항목을 놓쳤다** — 2026-08-17 에
`c7i.4xlarge`(물리 8코어)를 45분 띄우고도 **R6 를 안 태우고 껐다.** 목록에 없어서가 아니라
목록이 여러 곳에 있어서다.

이 문서는 **재고표**다. 「무엇을 만드나」는 [`db-portfolio-roadmap.md`](./db-portfolio-roadmap.md),
「EC2 를 띄울 때 뭘 같이 태우나」는 [`AWS-RIDE-ALONG.md`](../../loadtest/AWS-RIDE-ALONG.md) 다.
여기는 **「무엇이 아직 안 재졌나」** 만 본다.

🔴 **「측정이 남은 것」과 「구현이 남은 것」은 다른 일이다.** 아래는 **측정** 기준이고,
구현이 선행인 항목은 그렇게 표시했다. 계약·오류 처리·보안 결함은 §4 로 뺐다.

---

## 1. 🗄️ MySQL — 축이 제일 비어 있다

DBA 지원축의 결손 3개(백업 · 복제 · 무중단 DDL) 중 **복제만 아직 0** 이다.
백업은 [P3·P3-b](../../loadtest/results/backup-restore-aws-b-2026-08-13/README.md),
무중단 DDL 은 [P1](../../loadtest/results/online-ddl-aws-2026-08-12/README.md) 이 실측을 갖고 있다.

| 우선 | 실험 | 상태 | 왜 지금 |
|---|---|---|---|
| **1** | **P4 — 복제 지연 · 반동기 대가** | 🟡 [설계 완료](./replication-lag-and-semisync.md) · **rig 없음** | **결손 3축 중 유일한 공백.** ⭐ 반동기 대가는 **AZ 간 RTT 가 지배**하므로 로컬에선 구조적으로 과소평가된다 → **인스턴스 2대 필수.** 리드타임이 길다(rig 부터) |
| ~~#204~~ | ~~**[#204](https://github.com/Shadowfit/init/issues/204) 리포트 핵심 쿼리**~~ | ✅ **완료 (2026-08-19~20)** — [결과](../../loadtest/results/report-query-explain-2026-08-19/README.md) | 세 주장 중 **둘은 확인, 하나는 열쇠가 이미 있었다.** ㄱ 프루닝 — 세 팔 전부 **14파티션 전탐색**이고 인덱스로는 못 고친다(`created_at` 을 같이 넘긴 판만 1개). ㄴ 비커버링 — 논리 페이지 **3,777.5 ↔ 779(4.85배)**, 행당 4페이지. ㄷ filesort — A 에서 확인되지만 **[#188](https://github.com/Shadowfit/init/issues/188) 의 `uk_pose_event` 가 정렬 순서를 그대로 가지고 있어** B 에서 사라진다. 🔴 단 V6 은 `origin/main` 에 없다 — 지금 배포본엔 ㄷ 가 살아 있고, **#188 이 머지되면 손 안 대고 닫힌다** |
| **2** | **[#205](https://github.com/Shadowfit/init/issues/205) 포폴 카드 쿼리 3개** | 🟡 **막던 전제 둘이 풀렸다 (2026-08-20)** · 본 측정 미실행 — [결과 §5](../../loadtest/results/report-query-explain-2026-08-19/README.md) | #204 rig 을 만들다 **카드 B·C 가 현재 데이터로 구조적으로 못 돈다**는 것이 나왔다 — B 는 rep 당 프레임이 **30 고정**이라 두 평균이 같은 값이 되고, C 는 **겹치는 쌍이 0** 이었다. 둘 다 데이터를 채워 풀었다(B: 73.917431 ↔ 74.000000 으로 갈림 · C: 겹치는 쌍 26,847). **이제 돌리면 된다** |
| **3** | 🆕 **커버링 인덱스의 쓰기 대가** | 미측정 · **오늘 열림** | #205 **카드 A 의 남은 반쪽**이다. 읽기 이득은 쟀는데(4.85배) **채택 판단은 여전히 열려 있다** — #204 §2 가 «지금 판단하지 않는다» 라고 적은 그대로다. 참고로 이 박스에서 **인덱스 빌드만 1분 36초**(150만 행) |
| ~~#272~~ | ~~**`uk_pose_event` 의 쓰기 대가**~~ | ✅ **완료 (2026-08-18)** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/R8-unique-index-cost.md) | **처리량 −2.9% · p99 +98% · 버퍼풀 읽기 +17.5%.** 대가가 평균이 아니라 **꼬리**에 있다 — 「3% 느려진다」로 인용하면 안 된다 |
| **4** | 🆕 **유니크 키 대가 @ 버퍼풀 초과 규모** | 미측정 · **오늘 열림** | R8 에서 `Innodb_buffer_pool_reads` 가 **양쪽 다 0** 이었다 — 인덱스가 메모리에 다 들어가는 규모라 **디스크를 안 쳤다.** 관측된 +17.5% 는 메모리 안의 비용이고, **버퍼풀을 넘으면 대가가 훨씬 커진다.** 그 지점은 안 봤다 |
| **5** | 🆕 **내구성 완화가 왜 «더 느린가»** | 원인 미상 · **오늘 열림** | R4 에서 fsync 를 10배 걷어냈는데 처리량 **−15%**, p99 **3.5배 악화**, `log_bytes` 는 오히려 증가. 「3.47배가 1.03배로 무너진」 계열인데 **이번엔 부호가 음수**다. `sync_binlog=0` 의 몰아치는 flush 가 후보지만 미검증 |
| 6 | **[#219](https://github.com/Shadowfit/init/issues/219) `INSERT IGNORE` 전수 확인** | ✅ **감사 완료 (2026-08-18)** | 자리는 **한 곳뿐**이고 FK·NOT NULL 위험은 **이미 막혀 있다**. 🔴 대신 둘이 열렸다 — `sync_rate_at_trigger DECIMAL(5,2)` 에 **검증 없는 double**, `occurred_at` 미설정이 **1970 으로 뭉쳐 멱등을 흔든다**. 단 이 경로는 **한 번도 안 불린다**([#193](https://github.com/Shadowfit/init/issues/193)) |
| 7 | **pose_data 행 모양 × 파티션 상호작용** | 미검증 | DELETE 파편화 자체는 닫혔다(FIFO 누적 없음 · 구멍 뚫기 +24% 계단 1회). 이 **조합**은 안 봤다 |

⚠️ **#205 는 합성 데이터의 한계를 먼저 읽을 것** — 로드테스트 rig 은 단일 템플릿 복제라
**값 분포가 균일**하다. 옵티마이저 카디널리티·generated column 선택도처럼 **분포에 의존하는
실험은 지금 rig 으로 못 한다**(그 사실 자체가 이미 박제돼 있다). #204 판은 이 선을 지켰다 —
**계획 모양·접근 행수·페이지 수만** 말하고 카디널리티 «정확도» 는 결론 내지 않았다
([결과 §4-4](../../loadtest/results/report-query-explain-2026-08-19/README.md)).

---

## 2. 🌱 Spring — **실측은 하나 남았다**

> ✅ **2026-08-18 P5 라운드로 이 축이 사실상 비었다.** 아래 다섯이 한 라운드에서 닫혔고,
> 남은 실측은 **HTTP 부하 하 p99**(SLO 11번) 하나다 — 그건 의도적으로 뺐고(도구·인증 선행),
> E4 기준으로는 «안 했습니다, 이유는 X» 로 닫히는 형태다.

| 우선 | 실험 | 상태 | 왜 지금 |
|---|---|---|---|
| ~~H3~~ | ~~**캡이 «옆» 을 지키는가**~~ | ✅ **완료 (2026-08-17) · 반증** — [결과 §2](../../loadtest/results/coresidency-aws-2026-08-17/README.md) | **옆은 애초에 안 아팠다**(AI 세션 20→160 동안 옆 p50 8.2~8.8ms 평평 · fail 0). 캡을 걸어도 안 좋아지고(오히려 p99 10.7→12.7) **대가만 확실하다**(AI 처리량 −9.7% · AI p50 3.1배). [#254](https://github.com/Shadowfit/init/issues/254) 닫힘, [#212](https://github.com/Shadowfit/init/issues/212)은 **전제를 다시 세워야** 한다 |
| ~~P5~~ | ~~**세션 분산도 스윕**~~ | ✅ **완료 (2026-08-18)** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/README.md) · 36판 전부 유효 | **plateau 는 100이 아니라 10에서 붙는다**(266→1,013 RPS, 그 위 평탄). 기제는 락 대기 **29,999 → 40**(750배)·fsync 5배 감소. Q4 **번짐 없음**(레벨 1에서 옆이 가장 빠르다 4ms). 🔴 정본 649.4 의 «100세션» 은 필요 이상이었다 |
| ~~P2~~ | ~~**다운샘플 다세션 재측정**~~ | ✅ **완료 (2026-08-18)** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/P2-downsample-multisession.md) | **1.7배가 아니라 4.11배**(요청 처리량). 🔴 저장 처리량은 방향이 **뒤집힌다**(0.82배) — R=1 은 «느린» 게 아니라 «행을 5배 넣는» 것이다. p99 는 9.5배. `one-pager` 갱신 완료 |
| ~~#221~~ | ~~**`rewriteBatchedStatements` 기여분**~~ | ✅ **완료 (2026-08-18) · 이득 없음** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/README.md) | 8판 대조에서 **true 가 오히려 나쁘다**(p99 486 ↔ 261, 4판 전부 일관). `application.yml` 주석 정정 완료. ⚠️ 끄는 결정은 별도 |
| ~~#211~~ | ~~**hibernate `batch_size`**~~ | ✅ **닫힘 (2026-08-18) · 측정 항목이 아니었다** | 엔티티 **11개 전부 IDENTITY** 라 insert batch 가 애초에 꺼져 있고, §7.5 개선은 `JdbcTemplate` 이 낸 것이다(설정이 공을 잘못 가져갔다). `application.yml` 주석 정정으로 닫힘 |
| ~~#207~~ | ~~**타임아웃 스윕 메모리**~~ | 🟡 **시도했고 «못 쟀다» (2026-08-18)** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/R9-207-scheduler-and-ai-stop.md) | `jvm_gc_memory_allocated` 는 **GC 가 떠야 갱신**되는 파생 지표라 10,000 이하 팔이 전부 0 이다. 말할 수 있는 건 «10,000 이하는 62초에 GC 를 못 일으킨다» 뿐. **지표를 바꿔야 다시 잰다** |

---

## 3. 🤖 AI — 가장 큰 미지수가 여기 있다

| 우선 | 실험 | 상태 | 왜 지금 |
|---|---|---|---|
| **1** | 🔴 **「346 RPS 천장에서 서버가 9.5 vCPU 만 쓰는 이유」** | 🟡 [분석 완료 · 미결정](./ai-receive-path-scaling.md) · **rig 없음** | 하루에 **후보 일곱이 지워졌다**(GIL(추론) · 하드웨어 · 검출기 풀 · 부하기 CPU · anyio 40 · 부하기 프로세스 · 락). 남은 것은 **서비스 경로 GIL**(1순위, §10-1)과 **수신 경로 단일 이벤트 루프**다. 🔴 **py-spy 로는 더 못 간다**(§11) — **앱에 붙이는 계측**부터다 |
| ~~R6~~ | ~~**GIL 이냐 캐시냐**~~ | ✅ **완료 (2026-08-17) · GIL 반증** — [결과 §1](../../loadtest/results/ai-scaling-aws-2026-08-17/README.md) | 프로세스/스레드가 전 구간 **0.96~1.06x**(판정선은 1.7~2배였다). 스레드도 16워커에서 **7.08배** 확장하고, 순수 추론은 이 박스에서 **15.2 vCPU** 를 쓴다 — 즉 «8.69 vCPU» 는 추론 경로의 한계가 아니다 |
| ~~R7~~ | ~~**`model_complexity` 0/1/2 배수**~~ | ✅ **완료 (2026-08-17)** — [결과 §2](../../loadtest/results/ai-scaling-aws-2026-08-17/README.md) | c7i 배수는 **lite 0.79x · heavy 2.76x**(로컬 0.64x·3.83x) — **로컬 배수를 인용하면 안 된다**가 확인됐다. 🔴 덤이 더 셌다: **해상도를 9배 줄여도 추론 시간이 그대로다**([#92](https://github.com/Shadowfit/init/issues/92)) — 「해상도 낮춰 AI 를 싸게」는 길이 **없다** |
| ~~Q5~~ | ~~**관측 스택 동거 비용**~~ | 🟡 **쓰기 경로판만 완료 (2026-08-18)** — [결과](../../loadtest/results/session-spread-aws-2026-08-17/README.md) | 처리량 차이 **−0.05%**(판 간 산포보다 작다) · 관측 스택 CPU **1% 미만**. 「비싸서 볼 때만 켠다」의 근거가 사라진다. 🔴 **원 설계의 Q5(AI 부하 기준)는 여전히 미측정** — 두 부하는 병목이 다르다 |
| **3** | 🔴 **라운드 간 절대값 비재현 (+17.7%)** | **원인 가설 없음** · 2026-08-17 라운드로도 안 풀렸다 | AI CPU 는 869.3%→869.0% 로 **같은데** 처리량만 +17.7%(천장 환산 89.2→105.0세션). **이게 안 풀리면 모든 절대 용량 수치의 신뢰가 흔들린다.** 설계부터 필요 |
| **4** | 🆕 **AI 통보가 규모에서 어긋나는 이유** | 원인 미상 · **오늘 열림** | [#9 결과](../../loadtest/results/session-spread-aws-2026-08-17/R9-207-scheduler-and-ai-stop.md): 세션 1,000개를 한꺼번에 타임아웃시키면 **FAILED 980 인데 `ai_stop_result` 는 280**(71% 미기록). 10개에서는 1:1 이다. 후보 셋(비동기·예외 삼킴·틱 밀림) 중 **하나도 확인 안 했다** |
| 5 | **[#217](https://github.com/Shadowfit/init/issues/217) 무릎 각도의 z 의존** | 미검증 | 같은 프레임이 **3D 108° · 2D 178°** 로 갈린다 — 정확도 축의 뿌리 |
| 6 | [#234](https://github.com/Shadowfit/init/issues/234) · [#256](https://github.com/Shadowfit/init/issues/256) 정답지 흔들림 → 점수 영향 | 미측정 | **R7 «채택» 의 선행이다** — 배수는 이제 실측(lite 0.79x)이 있고, 남은 것은 **대가가 정확도**라는 쪽이다 |

---

## 4. 측정이 아니라 «구현·계약» 인 것 (별도 축)

여기 것들은 **재는 문제가 아니라 없는 문제**다. 측정 계획에 섞으면 우선순위가 왜곡된다.

| 구분 | 항목 |
|---|---|
| 🔴 **보안 — 측정보다 먼저** | [#187](https://github.com/Shadowfit/init/issues/187) AI HTTP 세션 소유권 검증 부재(번들 토큰만 있으면 **남의 session_id 로 주입**되고 Spring DB 까지 간다) · [#185](https://github.com/Shadowfit/init/issues/185) refresh token 평문 저장 |
| 🔴 **신뢰성 — 재전송의 선행** | 🆕 [#276](https://github.com/Shadowfit/init/issues/276) **동시 삽입이 데드락**(c=10 에서 34.8% 실패, 실측). ✅ **재현·조건 판별 완료 (2026-08-20, 로컬 팔 넷 16판)** — [결과](../../loadtest/results/r276-deadlock-2026-08-20/README.md): **세 조건이 «동시에» 성립할 때만 난다** — ① `uk_pose_event` 존재 ② 서로 **다른** 키 ③ **같은 파티션** 동시 삽입. 하나만 빠져도 0% 다(100% 중복 단일세션 **0%** · 파티션 가름 **0%** · **키 뺀 팔 0%**, 같은 파티션 **37.5%**). 🔴 **키를 뺀 팔은 삽입이 40배인데도 0** 이라 「다투는 자리가 `PRIMARY` supremum」 가설은 **반증**됐다 — 자리는 `uk_pose_event` 쪽이고, **이슈 제목이 맞다**. 🔴 **이슈가 기대한 완화는 성립하지 않는다** — 파티션이 월 단위라 동시 사용자는 전부 같은 파티션이다. 남은 미검증: 잠금의 정확한 기제(`SHOW ENGINE INNODB STATUS` 미확인) · 워커 수 스윕 · 재시도 간격. 재시도를 안전하게 만들려고 넣은 장치가 정작 재시도에서 500 을 낸다 — **[#188](https://github.com/Shadowfit/init/issues/188) 을 구현하기 전에 풀어야 한다** |
| 신뢰성 | [#188](https://github.com/Shadowfit/init/issues/188) 재시도 부재(rep 하나가 통째로 사라진다) · [#206](https://github.com/Shadowfit/init/issues/206) gRPC 예산 미전파 · [#208](https://github.com/Shadowfit/init/issues/208) 종료 정책 3갈래 |
| 계약 | [#209](https://github.com/Shadowfit/init/issues/209) 핸들러 4개 중 1개만 클라이언트 잘못을 `INVALID_ARGUMENT` 로 낸다 · [#218](https://github.com/Shadowfit/init/issues/218) 국면 이름표와 rep 판정이 다른 자를 쓴다 |
| 기능 부재 | [#193](https://github.com/Shadowfit/init/issues/193) · [#228](https://github.com/Shadowfit/init/issues/228) 자세 문제 유형 감지기가 **통째로 없다** |
| 구조 | [#176](https://github.com/Shadowfit/init/issues/176) · [#175](https://github.com/Shadowfit/init/issues/175) · [#174](https://github.com/Shadowfit/init/issues/174) · [#173](https://github.com/Shadowfit/init/issues/173) |

---

## 5. 순서 — 값싼 것과 값비싼 것

0. ~~**2026-08-17 라운드**~~ ✅ **회수 완료** — H3(반증) · R6(GIL 반증) · R7(배수 재측정)이
   한 번에 닫혔다. R6·R7 은 라운드 끝 **15분**에 AI 축 둘을 닫았다 — 예상대로 **단위 시간당
   가장 값쌌다.** 대신 AI 축에 **새 1순위**가 열렸다(「9.5 vCPU 의 이유」)
1. **P4 복제** — DBA 결손을 메우는 유일한 항목. rig 부터라 리드타임이 길다. **가장 먼저 착수**할 것
2. ~~**#204**~~ ✅ **회수 완료 (2026-08-20)** — 읽기축의 첫 실측이 들어왔다. 다음은 **#205**
   (전제는 이제 갖춰졌다)와 **커버링 쓰기 대가**다. 둘 다 로컬로 되고 포폴 결과물로 직결된다
3. **P5 · P2** — 둘 다 **이미 서류에 나간 정본 수치의 조건을 고치는** 일이다. 우선순위를 낮게 두면
   안 된다. 🔴 **H3 이 닫힌 지금 Spring 축에서 「rig 이 다 돼 있는데 안 돌린 것」은 P5 하나뿐**이고,
   從 R4(커넥션 스윕)는 **그 라운드에만** 얹힌다
4. **AI 「9.5 vCPU 의 이유」** — 후보 일곱이 지워지고 둘이 남았는데 **잴 도구부터 없다**(py-spy 한계).
   앱 계측을 넣는 일이라 **구현이 선행**이다
5. **절대값 비재현** — 위 전부의 신뢰를 떠받치는 바닥인데 **원인 가설조차 없다.** 설계부터고,
   라운드를 여러 번 돌려야 할 수 있다 — 이 목록에서 **가장 비싸다**

---

## 결정 로그

- **2026-08-20: #204 회수 + #276 조건 판별.** 읽기축에 **첫 실측**이 들어왔다(프루닝·커버링 확인, filesort 는 #188 키가 이미 닫고 있었다). 덤으로 **#205 의 막힌 전제 둘**이 같이 풀렸다. 그리고 #276 은 **조건이 갈렸다** — 키·서로 다른 키·같은 파티션 **셋 다** 있어야 나고(⚠️ 그날 1차 라운드가 「키는 무관」으로 잘못 읽힐 결론을 냈다가 키 뺀 대조군에서 뒤집혔다), 「실사용은 흩어진다」는 완화 기대가 월 단위 파티션에서 **성립하지 않는다**. ⚠️ **그날 첫 #276 판은 무효였다** — 유효 판 0 인데 집계가 `0.0%` 로 찍어 「데드락 없음」처럼 보였다. [#271](https://github.com/Shadowfit/init/issues/271) 과 같은 계열이라 그물을 하나 더 걸었다.

- **2026-08-18: P5 라운드 회수.** 主 둘(P5·P2) + 從 일곱이 **한 라운드에서** 닫혔다 —
  탑승 목록 사상 최대다. 그리고 **믿고 있던 것 넷이 뒤집혔다**: 「100세션이 baseline 조건」
  →10이면 충분 · 「rewriteBatchedStatements 는 개선」→이득 없음 · 「관측 스택은 비싸다」
  →1% 미만 · 「커넥션은 fsync 에 가려진 것」→원래 레버가 아님.
  🔴 **닫은 만큼 열렸다** — 새 항목 셋(버퍼풀 초과 규모의 유니크 키 대가 · 내구성 완화 역전
  원인 · AI 통보 누락 원인). §0 이 예언한 순환 그대로다.
  ⚠️ **rig 이 스스로를 열 번 넘게 잡았다**(디스크·NULL 산술·카운터 자리·권한 3종·워밍업
  동시성·CTE 한도·부팅 중 지표·이중 실행). 오염된 채 표에 들어간 것은 `s10_r5` 한 판뿐이고
  그것도 잡아서 뺐다 — 오늘 아침 [#271](https://github.com/Shadowfit/init/issues/271) 이
  «표는 정상인데 재는 게 없는» 실패를 보여준 뒤로 그물을 계속 늘린 결과다.


- **2026-08-17: P5 를 다음 EC2 라운드의 主 로 확정** (사용자 결정). 근거는 「값싸서」가 아니라
  **Spring 축에서 rig 이 완비됐는데 한 판도 안 돌린 유일한 항목**이고, 그 한 판이 없어서
  **정본 baseline 649.4 RPS 의 «100세션» 이 잰 값이 아닌 채로** 서류에 나가 있다는 것이다.
  從 R4(커넥션 스윕)는 부하기·페이로드가 이 rig 것이라 **이 라운드에만** 얹힌다.
- **2026-08-17(밤): 라운드 회수분 반영.** 문서를 만든 그날 셋이 닫혔다 — **H3**(Spring 1순위,
  반증) · **R6**(GIL 반증) · **R7**(c7i 배수). 표가 반나절 만에 낡았고, 「다음에 뭘 재나」를
  물었을 때 **이미 닫힌 항목이 1순위로 보였다.** 그래서 지우지 않고 «완료» 로 눕혔다.
  ⚠️ **닫힌 자리에 빈칸이 생기지 않았다** — AI 축은 후보가 일곱 지워진 대신 **새 1순위**가
  섰고, 그건 측정이 아니라 **계측을 만드는 일**이다.
- **2026-08-17: 문서 신설.** 「다음에 뭘 재나」가 이슈 40여 건 · 탑승 목록 · 설계 문서 다섯 곳에
  흩어져 있어 물을 때마다 다시 모으고 있었다. 직접적 계기는 **같은 날 `c7i.4xlarge` 를 45분
  띄우고도 R6 를 안 태우고 끈 것** — 목록에 없어서가 아니라 목록이 여러 곳에 있어서였다.
  ⚠️ **이 문서는 원본이 아니다.** 개별 결함의 원본은 이슈, 탑승 판단의 원본은
  `AWS-RIDE-ALONG.md` §1 從 표다. 여기 요약이 낡으면 **원본을 열 것**.
