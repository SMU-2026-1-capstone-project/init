# 아키텍처 회고 — 결함 · 다시 조립한다면 · 점수

작성일: 2026-08-11
상태: **분석/회고** — 새 결정 없음. 점수는 **측정이 아니라 판단**이고 기준을 §3 에 명시한다 ([[feedback_user_decides_not_claude]])
대상: E4(«모른다» 로 답할 자리가 없다). *"설계에서 아쉬운 점은?"* 은 면접에서 거의 확실히 나오는데 지금 답이 없다
연관: [`./project-destination-and-exit-criteria.md`](./project-destination-and-exit-criteria.md),
[`../tasks/32-deferred-items.md`](../tasks/32-deferred-items.md),
[`./outbox-reliable-messaging.md`](./outbox-reliable-messaging.md),
[`../../loadtest/results/ai-concurrency-2026-08-09/README.md`](../../loadtest/results/ai-concurrency-2026-08-09/README.md)

---

## 0. 현재 구조

```
폰(RN) ──프레임마다 base64 POST (3fps)──▶ FastAPI(MediaPipe, 상태 보유)
                                            │ rep 완성 시 배치 gRPC
                                            ▼
                                          Spring ──▶ MySQL
                                            │ outbox
                                            └──gRPC 통보──▶ FastAPI
```

---

## 1. 결함

### 🔴 최대 결함 — AI 가 stateful 인데, 그걸 전제한 설계가 없다

`ai-server/app/grpc/session_state.py` 가 세션 상태를 **프로세스 메모리에** 들고 있다.
아래 넷은 별개 문제로 보이지만 **뿌리가 하나다** — «실시간 세션» 이라는 상태를 **누가 소유하는지가 정해져 있지 않다.**
지금은 프로세스 메모리가 *우연히* 소유 중이다.

| 증상 | 근거 |
|---|---|
| AI 재시작 = 분석 결과 유실 | [`outbox-reliable-messaging.md §3-2`](./outbox-reliable-messaging.md) — 이미 문서화된 한계 |
| **무중단 배포 불가** | 배포하면 진행 중 세션이 전부 죽는다 |
| 스케일아웃 불가 | 어피니티가 없어 프레임이 상태 없는 인스턴스로 간다 |
| 검출기가 세션이 아니라 **스레드**에 붙음 | [#164](https://github.com/Shadowfit/init/issues/164) — `mediapipe_detector.py:107`(스레드 로컬) × `pose.py:45-52`(스레드풀) |

> **상태의 «소유 단위» 가 틀렸다.** 세션 상태인데 소유자가 프로세스와 스레드다.

### 그 외

| # | 결함 | 근거 | 성격 |
|---|---|---|---|
| ② | **쓰고 안 읽는 데이터가 시스템 볼륨의 대부분** | `PoseDataService.java:42`·`:221` 은 쓰는데, `PoseFrameProjection.java:18` 이 읽기에서 제외하고 API 노출은 0. 그런데 `SessionAnalysisCalculator.java:156` 은 용도(«앱이 그릴 자세»)를 명시한다 | **정합성** 결함 (성능 아님). 1억 행이 한 번도 안 읽힌 채 `DROP PARTITION` 으로 지워진다 |
| ③ | **배포 대상이 없다** | `.github/workflows/cd-backend.yml:6` — *"배포 대상 호스트가 아직 없기 때문이다(상시 EC2 인스턴스 없음)"*. CD 는 이미지만 굽는다 | «운영» 이라 부를 게 없어 SLO·용량·CPU 목표가 전부 가정 위 |
| ④ | **관측이 시스템 경계를 못 넘는다** | [`observability-correlation-id.md §6`](./observability-correlation-id.md) — 분산추적 **의식적 제외** | 병목이 AI 인데 **AI 구간을 못 본다.** 의도적 선택이었으나 지금은 걸린다 |
| ⑤ | ~~**관측 스택이 제품과 같은 박스**~~ | 🔴 **2026-08-11 철회 — 결함이 아니다.** `docker-compose.yml:130·150·188` 이 관측 3종을 `profiles: ["obs"]` 로 이미 분리해 뒀다. **기본 구동은 3개**(mysql·backend·ai)다 | 오히려 잘한 것 쪽이다. compose 주석이 이유까지 적는다 — *"profile 로 뺀 이유는 취향이 아니라 측정 오염이다… 그래프를 보려고 켠 것이 그래프를 못 믿게 만드는 상황을 만들지 않는다"* |
| ⑥ | **클라이언트에 백프레셔가 없다** | `frontend/app/(tabs)/exercise.tsx:195` — `setInterval(tick, 330)` 인데 `tick` 이 async 이고 **in-flight 가드가 없다** | 🔴 **AI 가 포화될수록 클라가 요청을 더 쌓는다.** 느려지면 줄여야 하는데 그대로 밀어넣는다 |
| ⑦ | **프레임을 base64 HTTP POST** | `exercise.tsx:160` `base64: true` | ⚠️ **미측정.** 스트리밍이 자연스러운 자리지만 비용을 잰 적이 없다 |
| ⑧ | **테이블 성격 경계가 흐림** | `exercise_references`(기준인데 프레임당 1행 = 대용량) · `daily_logs`(사용자 입력+파생 집계 혼재) · `reports`(스냅샷인지 재계산 뷰인지 미정) · `authority`(PK 이름이 `log_id` 인데 로그가 아님) | |
| ⑨ | **마스터 시드 누락** | `INSERT INTO exercises` 가 마이그레이션·시드 어디에도 없다(전수 확인) | 새 환경에서 안 돈다 → **E1 직결**. [`32-deferred-items.md P2`](../tasks/32-deferred-items.md) |
| ⑩ | ~~**CORS 위험 조합(FastAPI·Spring 둘 다)**~~ | ✅ **2026-08-26 발견 즉시 수정.** `ai-server/app/main.py`·`backend/.../WebConfig.java` 둘 다 `allow_origins("*")`/`allowedOriginPatterns("*")` + `allow_credentials(true)` 조합이었다 — 이 조합에서 CORS 미들웨어는 요청의 Origin 을 그대로 반사(reflect)해 "자격증명 포함 요청을 아무 오리진에서나 허용"이 된다. 양쪽 다 쿠키를 안 쓰는 것을 grep 으로 확인하고(`Set-Cookie`·`withCredentials` 0건) `allow_credentials`/`allowCredentials(true)` 를 제거 | 두 서비스에 **같은 실수가 복제**돼 있었다는 게 핵심 — 한쪽을 베낀 설정이거나 같은 프레임워크 기본값 습관. Spring 쪽은 [#149](https://github.com/Shadowfit/init/issues/149)에 "하드닝, 판단 유보"로 이미 등록돼 있던 것을 마저 닫았다 |
| ⑪ | ~~**API 문서(OpenAPI/Swagger)가 환경 구분 없이 항상 열려 있었다**~~ | ✅ **2026-08-26 발견 즉시 수정.** FastAPI `/docs`·`/redoc`·`/openapi.json`, Spring `/v3/api-docs`·`/swagger-ui`(후자는 인증까지 면제하는 화이트리스트) 전부 dev/prod 구분 없이 열려 있었다. FastAPI 는 기존에 있었지만 안 쓰이던 `DEBUG` 플래그에 연결, Spring 은 profile 로 dev/prod 를 못 가르는 구조라(둘 다 `SPRING_PROFILES_ACTIVE: prod`) 별도 `SPRINGDOC_ENABLED` 로 게이트 | 두 서비스 다 "dev 기본값이 prod 로 새는" 같은 패턴. Spring 쪽이 더 심했다 — 문서 엔드포인트가 인증 화이트리스트에도 올라 있었다 |
| ⑫ | ~~**`POST /pose` 의 `image` 필드에 크기 제한이 전혀 없었다**~~ | ✅ **2026-08-26 발견 즉시 수정.** `ai-server/app/models/pose.py` 의 `PoseRequest.image`(Base64)에 `max_length` 가 없었고, uvicorn·nginx-ai(dev 전용, prod 는 [#552](https://github.com/Shadowfit/init/issues/552)로 아예 없음) 어디도 안 막아줬다. `AI_PUBLIC_TOKEN` 이 앱 번들에서 추출 가능한 값이라([`ai-auth-token-flow.md`](./ai-auth-token-flow.md)) 토큰만 있으면 임의 크기 페이로드를 반복 전송할 수 있었다. `max_length=20_000_000`(안전판 — 실측이 아니라 정한 값) 부여 | DoS 성격의 실무형 결함. §4-1 의 세션 상태 크기 실측(rep 당 ≈146KB 선형 증가, [#229](https://github.com/Shadowfit/init/issues/229))과 같은 축(AI 프로세스 메모리 압박)에 있다 |

### 잘한 것 — 점수를 매기려면 이것도 센다

- **outbox 로 dual-write 를 풀고, 그것이 «못 고치는 것» 까지 문서화**(§3-2). 한계를 먼저 말하는 건 신입 수준이 아니다
- **측정 기반 결정 문화** — `measure_admin_filter_explain.sh` 가 `ANALYZE TABLE` 을 선행하고(`:148`·`:264`),
  **general log 에서 실제 SQL 을 건져** EXPLAIN 에 건다(`:18`). *"EXPLAIN 은 «결과가 없는 것» 보다 나쁘다(그럴듯해 보이므로)"*(`:274`)
- **자기 주장을 스스로 반증한 기록** — 엔진 우위 3개([`mysql-vs-postgresql.md §2`](./mysql-vs-postgresql.md)), 파티션 pruning 이득 0
- **미검증을 미검증이라 쓰는 습관** — #164 이슈의 «⚠️ 확정하지 말 것» 캐비엇 3개
- 실제 병목을 잡음 — projection `−98.7%`(조건: precompute·폴백 경로 한정 — [조건표](../portfolio/realmysql-experiments.md)), keyset

---

## 2. 다시 조립한다면

**두 개만 바꾸면 나머지가 따라온다.**

### ① 프레임 경로를 스트림으로

```
폰 ══연결 1개 = 세션 1개══▶ AI 인스턴스
```

| 얻는 것 | |
|---|---|
| **끊김이 이벤트가 된다** | 지금은 클라가 요청을 멈춰도 서버가 모른다 → 좀비 세션. `PoseDataOrphanMonitor` 와 `lastActiveAt` 의 존재 자체가 이 문제를 겪은 증거다 |
| **상태의 주인이 명확해진다** | 연결 수명 = 세션 수명 |
| **서버가 먼저 말할 수 있다** | 지금 피드백은 **다음 프레임 응답을 기다린다**(최대 330ms 지연). 스트림이면 rep 완성·피드백·TTS 를 즉시 민다 |
| **base64 팽창 제거** | binary frame → payload 약 25% 감소 |
| **백프레셔** | ⑥ 이 자연스러운 기본형으로 해결된다 |

**구간별 선택**: 폰→AI 는 **WebSocket**(RN 내장 API. gRPC-Web 은 bidi 를 제대로 지원하지 않고 RN 네이티브 gRPC 는 생태계가 얇다),
AI↔Spring 은 **gRPC 유지**. 스키마가 필요하면 기존 `exercise.proto` 를 WS 위에 바이너리로 실으면 된다.

#### 🔴 이 문서를 쓰며 정정한 것 둘

1. **«WS 면 #164 가 구조적으로 사라진다» 는 과장이었다.**
   MediaPipe 추론은 blocking CPU 작업이라 `async def` 핸들러에서 직접 돌리면 **이벤트 루프를 막는다.**
   결국 `run_in_executor` 로 빼야 하고, **그러면 다시 스레드풀이라 검출기가 다시 스레드에 붙는다.**
   WS 는 #164 를 **고칠 수 있게 해줄 뿐** 자동으로 고치지 않는다 — 진짜로 풀려면 «연결마다 전용 워커» 까지 가야 한다.
2. **«무중단 배포 가능» 도 반대에 가깝다.**
   연결이 세션에 묶이면 배포할 때 그 연결을 끊어야 하고 drain 이 «가장 긴 세션» 만큼 걸린다.
   정확히는 — **지금은 조용히 깨지고, 스트림이면 명시적으로 끊긴다.** 후자가 낫지만 «무중단» 은 아니다.

#### 트레이드오프

| | |
|---|---|
| **재연결 로직을 클라가 짜야 함** | 🔴 모바일에서 제일 큰 비용. 셀↔와이파이·백그라운드 전환 |
| **관측 모델 재설계** | 🔴 관측 1차가 **요청 단위 correlation id** 로 만들어져 있다. 스트림은 «연결 단위 + 메시지 단위» 라 안 맞는다 |
| 테스트·디버깅 | curl 로 못 찌른다. 기존 `measure_*.sh` rig 가 안 통한다 |
| LB·프록시 | idle timeout 을 세션 길이에 맞춰야 한다. 기본값이면 조용히 끊긴다 |
| 에러 모델 | HTTP 상태 코드가 없다. 애플리케이션 레벨 프로토콜이 필요하다 |

> **성능이 이유라면 근거가 약하다** — 전송·인코딩 비용은 **측정된 적이 없다.**
> **구조가 이유라면 충분하다.** 그래서 «성능 최적화» 가 아니라 **«상태 모델 정정»** 으로 불러야 정직하다.

### ② 세션을 «AI 인스턴스에 배정된 것» 으로 명시

Spring 이 세션 시작 시 인스턴스를 배정하고 그 매핑의 진실 출처가 된다.

- outbox 통보 대상이 결정된다 (지금 outbox 는 **단일 gRPC 대상을 전제**한다)
- 인스턴스 사망 시 **명시적** 재배정 + reattach (지금은 조용한 유실)
- drain 이 가능해진다
- 입장 제한([`32-deferred-items.md P3`](../tasks/32-deferred-items.md))이 자연스럽게 여기 붙는다

> ⚠️ **억지로 stateless 로 만들면 안 된다.** BlazePose 트래킹은 본질적으로 stateful 이고
> `static_image_mode=True` 로 없애면 매 프레임 탐지기가 돌아 **비용이 오른다.**
> 상태를 없애는 게 아니라 **주인을 정하는 것**이 답이다.

### 그 외

- **raw 좌표는 «용도를 정한 뒤» 저장** — Tier 0(자세 그리기)을 정의하고 거기 맞춰 보존 정책([`32-deferred-items.md P5`](../tasks/32-deferred-items.md))
- **배포 대상 1대** — 0대에서 N대를 논하는 것을 멈춘다

---

## 3. 점수

⚠️ **측정이 아니라 판단이다.** 기준을 같이 적고, 2026-08-11 시점의 스냅샷이다.

### 프로덕션 기준

| 축 | 점수 | 근거 |
|---|:--:|---|
| 데이터 모델·쿼리 설계 | **8** | 실측 기반. projection·keyset·인덱스 통합 전부 근거 있음 |
| 문서·코드 정합성 | **8** | 근거 링크 · 미검증 표시 · 자기반증. **이 프로젝트의 최고 자산** |
| 신뢰성(메시지 전달) | **7** | outbox 있고 한계까지 명시. 다만 AI 상태 내구성 없음 |
| 제품 완성도 | **5** | 돌긴 하는데 E1 미검증, 쓰고 안 읽는 데이터(②) |
| 관측성 | **5** | correlation id 있음, 분산추적 없음(④), 관측이 제품과 동거(⑤) |
| 확장성 | **3** | stateful AI + 어피니티 없음 + 배포 대상 0 |
| 운영 성숙도 | **3** | 배포·백업·복제 없음, 마스터 시드 누락(⑨) |

**종합 5.5 / 10.**

낮아 보이지만 — **확장성·운영이 낮은 건 «못 한 것» 이 아니라 «아직 안 한 것»** 이다.
배포 대상이 없는 시스템에 운영 점수를 줄 수는 없다.

### 신입 포트폴리오 기준

**8.5 / 10.** 기준이 다르기 때문이다. 신입 포폴에서 희소한 건 기능 개수가 아니라 **«측정» 과 «정직»** 인데 그 둘이 최상위다.

- 대부분의 신입 포폴은 «만들었습니다» 에서 끝나고 «재봤습니다» 가 없다
- **«제 주장이 틀렸습니다» 를 문서로 남긴 포폴은 거의 없다**
- 미검증을 미검증이라 쓰는 습관은 **신뢰의 근거**가 된다

1.5점을 깎은 건 **읽히지 않기 때문**이다 — `−98.7%`·`625배` 를 알아볼 사람이 적고 화면으로는 아무것도 안 보인다
([`project-destination-and-exit-criteria.md §2`](./project-destination-and-exit-criteria.md)가 이미 지적한 그 문제).

---

## 4. 점수를 올리려면 — 그리고 올리는 게 맞는지

| 할 일 | 축 | 상승 | 비용 |
|---|---|:--:|---|
| **배포 대상 1대** | 운영 +2 · 확장성 전제 | 🔴 최대 | 상시 인스턴스 비용 |
| **백업 + 복구 리허설** | 운영 +1 | 중 | DBA 결손 ①. **핵심은 «복구를 해봤나»** — 복구 안 해본 백업은 백업이 아니다 |
| **P2 시드 수정** | 운영 +0.5 | 쌈 | E1 직결 |
| **P3 입장 제한** | 확장성 +1 | 중 | 확장을 못 해도 «한계를 알고 지킨다» 는 확장성의 일부 |
| **P1 재측정** | 확장성 +0.5 | 중 | **용량 한계를 모르면 확장성 논의 자체가 불가** |
| **상태 모델 정정**(§2) | 확장성 +2 | 🔴 큼 | 클라·서버·관측 동시 |

**배포 1대가 병목이다.** 운영 점수는 «운영 중인 것» 이 없으면 구조적으로 못 올리고, 확장성도 거기 묶여 있다.

### 다만 — 목적지에 맞는지는 별개다

확장성·운영 점수는 **실제로 운영해야** 오르는데, 이 프로젝트의 목적지는 **취업 포트폴리오지 운영이 아니다.**
§4 가 «더 높은 성능 숫자» 를 잘랐듯, 운영 점수 올리기도 같은 함정일 수 있다.

> **확장성을 3에서 6으로 올리는 것보다 «왜 3인지 정확히 아는 것» 이 낫다.**

---

## 5. 면접 답변 (E4)

### Q. 설계에서 아쉬운 점은?

> "AI 서버가 세션 상태를 메모리에 들고 있는데, 그걸 전제한 설계가 없습니다.
> 그래서 재시작하면 분석 결과가 유실되고, 무중단 배포가 안 되고, 스케일아웃이 안 되고,
> 검출기가 세션이 아니라 스레드에 붙는 문제까지 생깁니다 — **네 개가 별개로 보이지만 원인이 하나**입니다.
> 상태의 소유 단위를 «프로세스» 가 아니라 «연결» 로 옮기는 게 정답이라고 봅니다."

### Q. 이거 확장 되나요?

> "안 됩니다. 어피니티가 없어서 인스턴스를 늘리면 프레임이 상태 없는 쪽으로 갑니다.
> 고치려면 연결 모델을 스트림으로 바꿔야 하는데, 재연결 로직과 관측 모델을 다시 짜야 합니다.
> 그리고 지금 병목은 확장이 아니라 **추론 CPU** 입니다 — 서버급 CPU 에서 프레임당 17.6ms,
> **물리 코어당 동시 16.4세션(상한)** 을 실측했습니다. 가정한 피크 67세션이면 물리 8코어 한 대 규모라,
> 수평 확장은 용량이 아니라 **가용성** 근거로만 남습니다."

### Q. 이 좌표를 왜 저장하세요?

> ⚠️ **지금 답이 약하다.** 1억 행을 쓰고 안 읽고 지운다. [`32-deferred-items.md P5`](../tasks/32-deferred-items.md) Tier 0 을 만들면 화면으로 답이 된다.

---

## 결정 로그

- 2026-08-26: **새 결함 셋 발견 즉시 수정.** FastAPI·Spring "프레임워크 자체 설계" 리뷰에서
  ⑩(CORS 위험 조합, FastAPI·Spring 둘 다) · ⑪(API 문서 항상 노출, 둘 다) ·
  ⑫(`POST /pose` 입력 크기 무제한)를 찾아 그 자리에서 수정까지 마쳤다.
- 2026-08-11: **결함 ⑤ 철회** — 「관측 스택이 제품과 같은 박스」는 사실이 아니다.
  `profiles: ["obs"]` 로 이미 분리돼 있고 기본 구동은 3개다. 초판이 `docker-compose.yml` 의
  서비스 «정의» 수만 세고 «기본 구동» 여부를 안 봤다. 같은 착오로 「시연 프로파일 분리」를
  §2 에서 제안했던 것도 걷어낸다 — **이미 돼 있다.**
- 2026-08-11: 회고 작성. **새 결정 없음.** 결함 9건 · 재조립 2축 · 점수(판단) 기록.
  §2 에서 **직전 대화의 과장 2건을 정정**(WS 가 #164 를 자동 해결하지 않음 · 무중단 배포는 오히려 어려워짐).
  결함 ⑥(클라 백프레셔 없음)은 이 회고 중 새로 발견 — 스트림 전환과 **독립적으로** 고칠 수 있다.
