# pose_data 적재 다운샘플링("1초 평균") — 어디서·왜·할 것인가

작성일: 2026-06-12
상태: **분석/추천 (결정 전)** — ai-server 갈림길이라 손대기 전 확인 필수([[feedback_minimize_python_changes]]). 트레이드오프 박고 결정받기([[feedback_decision_doc]])
연관: [`db-portfolio-roadmap.md §10-③`](./db-portfolio-roadmap.md), [`db-deep-dive.md §2-0`](../portfolio/db-deep-dive.md)(raw→buffer 분리), [`realmysql-experiments.md §4-②a·④`](../portfolio/realmysql-experiments.md)(batch insert·trim), [`portfolio-narrative.md §2`](../portfolio/portfolio-narrative.md)(쓰기 축)

---

## 0. 한 줄

> 원래 "1초 평균 집계 = 저장공간 감축"으로 적었으나, 서사가 **raw=단기 버퍼(TTL)·derived=영속**(§2-0)으로 진화하면서 *영속 공간* 명분은 이미 해소됐다. **남은 진짜 효용은 "라이브 적재(write-heavy) 비용 감소" 하나**이며, 그게 사실이라면 **어디서(AI vs Spring) 다운샘플링하느냐**가 결정 대상이다.

---

## 1. 현재 흐름 (실코드 — 프레임당 1행)

```
폰 ──(프레임마다 base64 POST)──▶ FastAPI /pose
   MediaPipe detect → analyzer.process_frame
   visibility 통과한 매 프레임 → state.current_rep_frames.append(...)   # pose.py:91-96  ← 1프레임=1행
   rep 완성 시 → 그 rep의 모든 프레임을 batch gRPC 전송               # pose.py:107-116
        │  report_pose_data_batch(session_id, [PoseDataRequest ×N])
        ▼
   Spring PoseDataService.savePoseDataBatch
        jdbcTemplate.batchUpdate(INSERT ... )  # 1프레임=1 INSERT row     # PoseDataService.java:54
        ▼
   MySQL pose_data  (rep당 ~5~30행, §roadmap-5)
```

**확정 사실**: `pose_data`는 **프레임 단위로 1행씩** 쌓인다. "1초 평균/다운샘플"은 **현재 미구현**(roadmap §7 갭). DAU 1,000·1억 행 합성은 이 프레임 단위 가정 위 숫자다.

> 중요 분리: **라이브 분석(DTW·sync·rep 감지)은 저장 전 FastAPI에서 끝난다.** `pose_data`에 들어가는 건 *리포트(precompute)용 사후 데이터*. → **저장본을 다운샘플해도 라이브 분석 정확도엔 영향 없고, 영향받는 건 리포트 시계열 해상도뿐.** (핵심 안전판)

---

## 2. 재구성 — 다운샘플링이 *지금* 무엇을 위한 건가

원래 "1초 평균"이 뭉뚱그린 3가지 관심사를 분리하면:

| 관심사 | §2-0 진화 후 상태 | 다운샘플링이 필요? |
|---|---|---|
| **A. 영속 저장공간** (연 ~800GB raw 오염) | ✅ **이미 해소** — raw는 단기 TTL 버퍼(DROP PARTITION), derived만 영속(precompute) | ❌ 불필요 (TTL이 해결) |
| **B. 라이브 적재 비용** (write-heavy: INSERT 행수·gRPC payload·풀 압박) | 🔶 미해소 — 프레임당 1행이 그대로 DB·네트워크 부하 | ✅ **여기서만 유효** |
| **C. 리포트 시계열 해상도** | 현행 프레임 단위(과할 수 있음) | ⚠️ 다운샘플 시 해상도 trade |

→ **결론: 다운샘플링의 유일하게 살아있는 명분은 B(라이브 적재 비용).** 저장공간(A)으로 정당화하면 §2-0과 모순되니 **B로만 정당화할 것.** 그리고 B가 실제로 충분히 큰 문제인지부터가 사전 질문(§5 측정).

---

## 3. 결정 대상 — 어디서 다운샘플링하나

B(적재 비용)를 줄이기로 *가정*하면, 줄이는 지점은 셋:

| | A. FastAPI(AI)에서 | B. Spring INSERT 직전 | C. 안 함 (raw 그대로 + TTL) |
|---|---|---|---|
| 위치 | gRPC 전송 *전* 프레임 평균/추출 | `savePoseDataBatch` 안에서 평균 후 batchUpdate | 다운샘플 없음 |
| **gRPC/네트워크 절감** | ✅ 전송량 자체 감소 | ❌ 이미 다 받은 뒤 | ❌ |
| **DB INSERT 행수 절감** | ✅ | ✅ (실측 병목은 DB INSERT — §realmysql ②a) | ❌ |
| **ai-server 수정** | 🔴 **필요** (타인 설계, 확인 필수 [[feedback_minimize_python_changes]]) | 🟢 불필요 | 🟢 불필요 |
| **쓰기 축 서사 소유** | AI 쪽으로 분산 | 🟢 **Spring이 적재 최적화 소유**([[user_career_target]] 백엔드 포폴) | — |
| 리포트 해상도 통제 | AI가 정책 보유 | 🟢 Spring(저장 계층)이 정책 보유 | 원본 유지 |
| 라이브 분석 영향 | 없음(분석은 저장 전 완료) | 없음 | 없음 |

### 3-A. FastAPI에서 (네트워크까지 절감, 단 ai-server 침습)
- 장점: gRPC payload·DB 행수 **둘 다** 감소 — 가장 근본. 폰→AI는 어차피 못 줄이지만 AI→Spring 구간을 줄임.
- 단점: ai-server는 **타인 설계** → 무단 수정 금지, 협의 필요. squat 분석 로직과 같은 파일(`pose.py`)이라 결합 위험.
- 포지셔닝: 절감 효과는 최대지만 **백엔드(Spring) 포폴 서사로는 약함**(최적화가 AI 쪽 코드에 있음).

### 3-B. Spring INSERT 직전에서 (추천 후보 — 백엔드 소유)
- 장점: **ai-server 무수정**, 적재 최적화를 **Spring(쓰기 축)이 소유** → 포폴 서사 정합. 실측 병목이 **DB INSERT**(§realmysql ②a: batch로 throughput +99%)였으므로, 행수 감축은 *그 병목*을 직접 친다.
- 단점: gRPC/네트워크는 **이미 다 받은 뒤**라 절감 못 함(AI→Spring 구간 낭비 잔존). Spring CPU로 평균 계산.
- 확장성([[feedback_industry_level_standard]]): 동시 세션↑ 시 INSERT 행수가 풀·p99을 압박 — 여기서 감축이 효과. 단 네트워크가 병목이 되는 구간에선 B로 부족 → 그때 A 재고.

### 3-C. 안 함 (정직한 baseline)
- §2-0가 영속 공간을 이미 해결하므로, **적재 비용이 측정상 문제 아니면 다운샘플은 premature**([[redis-introduction]] 기조: 측정 전 최적화 금지).
- raw를 짧은 TTL 버퍼로 두고 DROP PARTITION으로 버리면, "프레임당 1행"이어도 영속 부담 없음. **B(적재 비용)가 실측으로 아플 때만** A/B 착수.

---

## 4. "1초 평균" vs "1초 1프레임 추출" (방식 자체)

> ⚠️ **2026-08-01 정정 — 이 절의 비교는 지금 데이터에서 성립하지 않는다** ([#79](https://github.com/Shadowfit/init/issues/79)).
>
> 아래 비교는 **프레임마다 `sync_rate` 가 다르다**는 전제 위에 서 있다. 실제로는 ai-server 가 **rep 단위로 채점해 프레임마다 복제**하므로(`pose.py:111`) 한 배치(=한 rep) 안에서 상수다. 그래서 "평균"과 "최악 sync 대표추출"이 **같은 값을 돌려주며 구분되지 않는다.**
>
> 코드도 마찬가지였다. `PoseDataService` 가 "최저 `sync_rate` 프레임을 대표로 남긴다"고 되어 있었지만 엄격 부등호가 참이 되는 경우가 없어 **매 window 의 첫 프레임이 그대로 남고 있었다** — 실제 동작은 균등 샘플링이었다. 지금은 코드·주석을 실제 동작에 맞췄다.
>
> **R≈5 라는 비율 자체는 유효하다.** §5-1(4) R-sweep 은 *"몇 개를 남기나"* 의 실험이고 #79 는 *"그중 어느 것을 남기나"* 의 문제라 결론이 뒤집히지 않는다.
>
> 아래 서술 중 **여전히 유효한 것**: `joint_coordinates` 평균의 의미가 모호하다는 지적. 좌표는 프레임마다 실제로 다르므로 "어느 프레임의 좌표가 남는가"는 지금도 의미가 있다 — 다만 그 선택 기준을 `sync_rate` 로 삼을 수 없다는 게 #79 다.

다운샘플을 한다면 *어떻게*도 갈린다:
- **평균(average)**: 1초 구간 N프레임의 좌표를 평균 → 1행. 노이즈 완화하나 **JSON 좌표 평균의 의미가 모호**(관절 위치 평균이 실제 자세 아님), sync_rate·feedback 같은 비수치 필드 병합 애매.
- **대표 추출(decimate/pick)**: 1초당 1프레임만 남김(예: 구간 중앙/최악 sync). 의미 보존·단순. **리포트가 "구간 대표"면 충분**이라 보통 이쪽이 정직.
- 추천 경향: "평균"이라는 이름에 갇히지 말고, **리포트가 요구하는 해상도에 맞춘 대표 추출**이 보통 더 안전(특히 worst-rep 분석은 평균이 아니라 극값이 중요 — §realmysql 리포트 집계).

---

## 5. 측정 먼저 (착수 전 — 말 아닌 증거)

B가 실제 문제인지부터 ghz rig로 확인(이미 보유):
- 현행 프레임당 1행에서 **동시 세션 N(→DAU 1,000 환산)별 INSERT throughput·p99·HikariCP active/pending** — §realmysql 배치 카드 재사용.
- 다운샘플(예: 1초 1행, ~Mx 감축) 적용 후 동일 측정 → **행수 1/M ↔ p99·풀 개선폭**.
- 네트워크: AI→Spring gRPC payload 크기 before/after (A안일 때만 의미).
- 판정: 감축이 p99/풀을 의미 있게 낫게 하면 착수, 아니면 C(안 함) 유지가 정직.

---

## 5-1. 측정 결과 (2026-06-12 실측) ⭐

rig: `loadtest/.bin/ghz.exe` → `SavePoseDataBatch`, 백엔드 :6565(JDBC batch 빌드), `pose_data` 실데이터 412만 행 위, 워밍업 후 각 n=500~600. **단일 머신 동거(4코어) — 절대 RPS는 오염, 신뢰는 *형태/상대*.**

### (1) baseline — 동시성 천장 ~25 RPS, p99 폭증
단일세션(801) 고정, c 스윕: RPS는 c=5~10에서 **~25에 평탄**, p99는 c=5 566ms → c=70 **11.6초**. 처리량 평탄 + 지연 폭증 = 포화. err=0(안 터지고 느려짐). ≈625 행/초 천장.

### (2) 병목 귀속 — 풀=10이 진짜 캡, 버퍼풀 가설은 반증
c=50 지속 부하 중 MySQL 내부 관측:
- **디스크 읽기 +2회**(45초간), 논리읽기 +34만 → **전부 인메모리. I/O 바운드 아님.** "버퍼풀 128MB라 디스크 친다" 가설 **반증**(단일세션이라 작업셋이 작아 풀 상주).
- **backend 커넥션 10개 전부 `Query`** = HikariCP 풀=10(Spring 기본). c↑여도 DB엔 항상 10개만 → **풀이 throughput 캡**.
- `data_lock_waits`=0(append라 행-락 무). `threads_running`≈11인데 CPU 1.5/4코어만 → **내부 직렬화 대기**(단일 session_id=801 인덱스 리프 래치·redo 커밋).
- **결론**: 천장 = ①풀=10 + ②단일세션 핫스팟(rig 결함) + ③동거. **INSERT 행수/디스크 아님.**

### (3) 다세션 rig — 천장은 그대로, 핫스팟은 꼬리지연만 부풀렸음
901~1900 세션 round-robin(아티팩트 #2 제거):

| c | 단일 RPS / p99 | 다세션 RPS / p99 |
|---|---|---|
| 30 | 19.7 / 6341 | 27.1 / **2159** |
| 100 | 22.7 / 10731 | 26.4 / **5004** |

→ **RPS 천장 불변(~25-29) = 풀이 캡**. 단일세션은 **p99만** 2~3배 부풀렸음(실제 다세션 워크로드는 꼬리지연 덜 나쁨).

### (4) R-sweep — 배치 비용은 고정비용 지배 → 다운샘플은 1순위 처방 아님 ⭐
배치당 행수 R을 25→5→1로(다세션, 풀=10):

| c | R=25 | R=5 | R=1 |
|---|---|---|---|
| 10 | RPS 29.7 / **742행/s** / p50 300 | RPS 48.3 / 242 / 195 | RPS 58.7 / 59 / 165 |
| 50 | RPS 17.8 / **445행/s** / p99 8007 | RPS 40.3 / 202 / 2363 | RPS 69.8 / 70 / 1068 |

- **고정비용 지배(sub-linear)**: R 5배 줄여도 배치지연 W는 5배가 아니라 ~1.8배만↓(1970→1094ms). rows/s는 폭락(742→59 at R1) → 배치 지연은 행수가 아니라 라운드트립·커밋·풀획득 같은 **고정비용이 대부분**. (batch insert +99%의 역증명 — 배치가 고정비용 분산.)
- **그런데 그 sub-linear 개선조차 처리량을 크게 올림**: 처리량=풀/W 라 W 1.8배↓ → **RPS c=50서 +126%(17.8→40.3), p99 −70%(8007→2363)**. 즉 "행수엔 둔감하지만 둔한 개선이 처리량을 배로." → **다운샘플은 진짜 쓰기축 레버**(앞서 "~35%·부수카드"는 저부하 p50만 본 오판, 정정).
- **수확체감 = 스위트스폿 ~R5**: R 5→1은 행 또 5배 줄여도 처리량 40→70만(rows/s 폭락) → **R=1 같은 과도 감축은 행당 최악.** "1초 평균"식 극단 금지 근거.

### (5) 풀 사이징 실측 — 천장은 풀이 아니라 **박스(2 물리코어)** ⭐ 반전
풀=30 컨테이너(별 포트) 재생성 후 다세션 동일 스윕:

| c | 풀=10 RPS / p99 | 풀=30 RPS / p99 |
|---|---|---|
| 10 | 29.4 / 1338 | 22.4 / 2857 |
| 50 | 20.6 / 4873 | 23.3 / 4504 |
| 100 | 26.4 / 5004 | 30.2 / 5212 |

- **풀 3배(10→30)에도 RPS 개선 0**(오히려 저~중 c에서 약간 악화), p99도 악화 → **천장은 풀이 아니라 박스 한계.** 풀=10 "10개 다 바빴음"은 *원인 아닌 증상*(박스가 그 이상 못 밀어냄).
- 추가 20커넥션 = 2코어에 컨텍스트 스위칭만↑(예측한 트레이드오프). HikariCP 스위트스폿 (물리2×2)+1≈5 → pool=10 이미 위, 30은 과함.
- **머신 한계 확인** = 동시성(30)을 늘려도 병렬성(2코어) 불변 → 처리량 못 늘고 꼬리지연만. 진짜 레버는 풀숫자가 아니라 **코어↑ / 수평확장**. ⚠️ 단 이 절대천장 자체가 **i3 2코어 동거 측정**이라 무의미([[project_loadtest_env_constraint]]) — 신뢰는 "풀↑이 안 통한다"는 *상대 결론*뿐.
- ⚠️ **추가 정정(2026-07-11)**: HikariCP 스위트스폿 공식(코어×2+스핀들)이 원래 가리키는 대상은 **DB 서버의 코어수**다 — 커넥션을 그 이상 열어봐야 DB가 동시에 처리할 수 있는 병렬도 이상으로는 대기열만 늘어난다는 논리. 이 실험은 mysql·backend가 **같은 물리 2코어를 공유**하는 로컬 환경이라, "코어"가 DB 서버 코어인지 백엔드(JDBC 클라이언트) 코어인지 구분이 안 된 채 "박스가 병목"으로 뭉뚱그려졌다. 실제 프로덕션처럼 앱 서버와 DB가 **분리된 인스턴스**(RDS 등 관리형 DB가 표준)라면 백엔드 코어수는 커넥션 풀 사이징과 무관해지고, DB 서버 자체의 코어·디스크 기준으로 재계산·재실측해야 한다 — 지금 "pool=10이 충분하다"는 결론은 **이 동거 환경에 종속**된 것이지 분리된 배포에 그대로 이전 가능한 결론이 아니다.

> 스크립트: `loadtest/ghz/ceiling.ps1`(단일), `gen_batch_multi.py`(다세션 array), 결과 `results/multi-c*.json`·`rsweep-c*-r*.json`·`pool30-c*.json`. 풀30 컨테이너는 일회용(측정 후 제거).

### (6) 분리 배포 가정 시 풀 사이징 — 이론 계산 (2026-07-11, 실측 아님)

**목적**: (5)의 정정에서 지적했듯, "pool=10 충분"은 mysql·backend가 같은 2코어를 공유하는 로컬 환경 종속 결론이다. 실제 프로덕션처럼 앱 서버와 DB가 분리된다면 재계산이 필요한데, 지금 이 프로젝트는 AWS에 실제 배포할 계획이 없어([[project_keep_server_ai_architecture]] 계열 방침과 동일한 이유로 실비용 인프라 전환은 안 함) 실측 대신 **가정 기반 이론 계산**으로 정리한다. §2-3-1/2-3-2(GPU 도입 사고실험)와 같은 패턴 — 실제 착수 대상이 아니라 면접 설계 서사용.

**가정**: 타깃 RDS 인스턴스 = `db.t4g.micro`(2 vCPU, Graviton, 버스터블). 근거: 이 프로젝트가 이미 못박아둔 DAU 1,000 가정([[feedback_state_assumption_design_to_it]])에 맞는 최소 현실적 등급 — 그 이상을 가정하면 과설계.

**계산**: HikariCP 스위트스폿 공식 = (코어수 × 2) + 유효 스핀들수. `db.t4g.micro` = 2 vCPU, RDS 스토리지는 SSD(gp3) 기반이라 스핀들=1로 단순화하면:

```
(2 × 2) + 1 = 5
```

**흥미로운 우연**: 로컬 실험(5)에서 나온 "pool=10 이미 스위트스폿(≈5) 위"의 그 5와 **숫자가 같다**. 하지만 이유는 완전히 다르다:
- 로컬: 2코어를 **backend와 mysql이 나눠 쓰는** 환경이라 그 2코어가 병목이었음(백엔드 JDBC 클라이언트 작업과 MySQL 서버 작업이 같은 CPU를 두고 경쟁).
- `db.t4g.micro` 가정: 2 vCPU는 **DB 전용**(앱 서버는 별도 인스턴스) — 백엔드가 뭘 하든 이 코어 수와 무관, 순수하게 "RDS가 동시에 처리할 수 있는 쿼리 병렬도"만 반영한 숫자.

**시사점**: `db.t4g.micro`에 배포한다고 가정하면 현재 코드에 명시된 `maximum-pool-size=10`은 이론상 스위트스폿(5)의 2배 — 로컬 실험이 보여준 "더 큰 풀도 해 안 됨, 컨텍스트 스위칭만 증가" 패턴이 여기서도 반복될 가능성이 있다는 뜻(단, 이번엔 병목 주체가 DB 서버 코어라는 점이 다름).

**정직하게 짚을 점 (실측 아님을 다시 강조)**:
- 이 계산은 **`db.t4g.micro`를 실제로 띄워 부하를 걸어본 게 아니다** — 순수 공식 대입.
- `db.t4g.micro`는 버스터블(T-series) 인스턴스라 CPU 크레딧이 있고, 베이스라인(약 10~20%) 초과 지속 시 스로틀링된다 — 이건 풀 사이징과는 별개의 제약이라 공식만으로는 안 잡힘.
- 공식 자체가 "짧은 트랜잭션형 쿼리" 워크로드를 전제로 함 — 이 프로젝트는 대체로 단순 조회·배치 INSERT라 전제에 부합하지만, 장시간 트랜잭션이 섞이면 공식이 안 맞을 수 있음.
- 이 이론 계산이 실제와 어긋나는지 확인하려면 결국 실제 `db.t4g.micro` RDS를 띄워 실측해야 하는데, 이건 실비용이 드는 별도 결정이라 착수하지 않음(사용자 confirm 대기 상태로 남김).

### (7) 분리 배포 실측 — EC2 2대로 (6)의 이론계산 검증, 로컬 결론 반전 (2026-07-25) ⭐

**목적**: (5)의 로컬 결론("풀 10→30 늘려도 개선 0, 천장은 박스")이 mysql·backend가 물리 2코어를 공유하는 동거 환경에 종속된 결론이라는 걸 (5)/(6)에서 이미 정정했다. 이걸 말로만 정정하지 않고, 실제로 DB와 백엔드를 별도 인스턴스로 분리해 같은 풀 10→30 비교를 재현했다.

**환경**: AWS `ap-northeast-2`, 임시 EC2 2대(테스트 종료 후 삭제).
- DB 전용: 처음 `t4g.small`(2 vCPU)로 시작했으나 로컬 docker-compose 설정(`innodb-buffer-pool-size=2G`)을 그대로 복붙해 RAM 1.8GB짜리 인스턴스에서 스왑 없이 메모리 고갈 → 인스턴스 자체가 응답불능이 돼 재부팅도 안 먹혀 재생성. 버퍼풀을 768M로 낮췄지만 이번엔 **컨테이너 자체의 `--memory` cgroup 캡(1.2GB)**을 호스트 확장과 별개로 놔둬서 부하 중 컨테이너가 OOM-kill(exit 137)당하는 일이 두 번 더 있었음 — 결국 인스턴스를 `t4g.medium`(2 vCPU, RAM 4GB)으로 올리고 컨테이너 메모리 캡도 3GB로 같이 풀어준 뒤에야 안정화. **핵심 교훈: 호스트 RAM만 올리고 컨테이너 cgroup 캡을 안 풀면 그대로 재발한다** — 이번 실험 자체의 방법론적 함정이라 정직하게 남김.
- App(백엔드+ghz): `t4g.medium`(2 vCPU, RAM 4GB) 별도 인스턴스, DB와 사설망(같은 VPC, 프라이빗 IP)으로 통신 — DB 코어를 전혀 공유하지 않음.
- 데이터: `pose_data` 375만 행 시딩(로컬 실험 "412만 행"과 비슷한 규모, rig A 방식 세션 cross join). 시딩 중 EBS 20GB가 가득 차 50GB로 확장하는 일도 있었음.
- 부하: 로컬과 동일한 다세션 rig(`gen_batch_multi.py`, 세션 1~1000 라운드로빈, R=25), ghz c=5/10/30/50/100 스윕(warmup c=20, 각 레벨 `-n 2000` 고정).

**결과 — pool=10 vs pool=30 (동일 분리 배포 환경)**:

| c | pool=10 RPS | pool=10 p99 | pool=10 실패 | pool=30 RPS | pool=30 p99 | pool=30 실패 |
|---|---|---|---|---|---|---|
| 5 | 1.7 | 14222ms | 102/2000 | 228.4 | 38ms | 0/2000 |
| 10 | 191.0 | 452ms | 0/2000 | 202.6 | 642ms | 0/2000 |
| 30 | 211.1 | 1108ms | 0/2000 | 203.9 | 1054ms | 0/2000 |
| 50 | 104.3 | 3142ms | 0/2000 | 187.0 | 1136ms | 0/2000 |
| 100 | 5.9 | 19825ms | 937/2000 | 188.3 | 1326ms | 0/2000 |

**해석 — 로컬 (5)의 결론이 뒤집힌다.**
- c=10~30(중간 부하)에서는 pool=10과 pool=30이 사실상 동등 — 여기까지만 봤다면 로컬 결론("풀 안 통함")이 재확인되는 것처럼 보임.
- 하지만 **c=50부터 갈라지고 c=100에서 극적으로 갈린다**: pool=10은 완전히 무너짐(RPS 5.9, DeadlineExceeded 47% — ghz 기본 20s 타임아웃 벽에 그대로 부딪힘), pool=30은 끄떡없음(RPS 188, 실패 0). c=100의 `DeadlineExceeded`는 서버 크래시가 아니라 클라이언트 20s 타임아웃에 걸린 것 — 대기열이 그만큼 깊어졌다는 뜻으로 읽어야 함(§5-1(1) baseline의 "err=0, 느려지기만" 패턴이 더 심해진 형태).
- **결론: DB가 자기 전용 코어를 가진 분리 배포에서는 풀 사이징이 고부하 구간(c≥50)에서 확실히 유효한 레버다.** (5)의 "풀 3배 늘려도 개선 0"은 로컬 동거 환경 자체의 부산물이었다는 걸 이번 실측이 직접 증명 — (6)의 이론계산이 예견한 "분리 배포라면 다시 계산해야 한다"는 방향이 맞았다는 뜻이기도 하다. 단 (6)의 스위트스폿 계산(≈5)과 정확히 일치하는지까지는 이번 실험이 답하지 못한다 — 테스트한 값이 10/30 두 점뿐이라 그 사이 최적점은 미탐색.

**c=5 이상현상에 대한 정정**: 처음엔 pool=10의 c=5가 재현성 있게 나빴어서(세 번 재시도해도 RPS 1.7~3.0, 14초대 p99) "낮은 동시성이 꼬리지연을 증폭시킨다"는 가설을 세웠다. 그런데 pool=30에서는 c=5가 오히려 전 구간 중 가장 좋았다(RPS 228). 다시 보니 이 스윕 스크립트의 warmup이 **c=20 고정**인데, pool=10일 때는 warmup 자체가 풀(10)보다 큰 동시성(20)으로 미리 포화시켜놓고 그 잔여 정체가 바로 이어지는 c=5 측정에 새어 들어간 것으로 보인다 — 즉 "낮은 동시성 자체의 성질"이 아니라 **이 측정 스크립트의 warmup 설계 결함(warmup 동시성이 테스트 대상 풀 크기보다 클 수 있음)**일 가능성이 높다. 확정하려면 warmup을 각 풀 크기 이하로 낮춰 재검증해야 하는데, 이번 실험 범위에서는 하지 않음 — 가설로만 남김.

**한계**:
- 절대 수치(RPS)는 여전히 참고용 — 프로덕션 RDS가 아닌 자체관리 MySQL on EC2(t4g.medium, 버퍼풀 768M)이고, 페이로드도 실제와 다른 합성 JSON.
- 테스트한 풀 값은 10/30 두 점뿐이었음 → (8)에서 15/20 추가 측정으로 보완.

### (8) 풀 사이징 cliff 위치 확인 — 15/20 추가 측정 (2026-07-25)

**목적**: (7)은 10과 30 두 점만 봐서 "그 사이 어디서부터 안 무너지는가"(cliff 위치)를 몰랐다. c=100(가장 극적으로 갈렸던 레벨) 고정, pool=15·20을 추가 측정해 cliff를 좁혔다.

**방법 변경**: (7)의 c=5 아티팩트 가설(warmup c=20이 pool=15보다 커서 오염 가능)을 피하려고, 이번엔 warmup을 **c=10**으로 낮춰(모든 테스트 풀 값보다 작게) 측정했다. c=100 레벨만 `-n 2000`으로 측정, 나머지 c는 재측정하지 않음.

**결과 — c=100 고정, 풀 크기별**:

| pool | RPS | p50 | p95 | p99 | 실패 |
|---|---|---|---|---|---|
| 10 | 5.9 | 14451ms | 19326ms | 19825ms | 937/2000 (47%) |
| **15** | 199.8 | 292ms | 1313ms | 1529ms | 0/2000 |
| 20 | 191.8 | 278ms | 1275ms | 1432ms | 0/2000 |
| 30 | 188.3 | — | — | 1326ms | 0/2000 |

**해석**: cliff는 **10과 15 사이**에 있다 — 15부터는 20·30과 사실상 동급(RPS 190~200대, p99 1.3~1.5초, 실패 0)이고, 30까지 늘려도 15 대비 이득이 없다(오히려 RPS는 15가 근소하게 더 높음 — 오차 범위 수준). 즉 **이 워크로드의 실측 스위트스폿은 ~15 근방**이며, (6)의 이론 공식(코어×2+스핀들 ≈5)보다 3배 가까이 크다. 이 워크로드(단순 batch INSERT, I/O·락 대기 비중이 큼)에서는 CPU 병렬도 기준 공식이 실제보다 훨씬 보수적으로 나온다는 뜻 — 공식은 출발점일 뿐 실측으로 보정해야 한다는 근거로 쓸 것.

**한계**: c=100 한 레벨만 봤다 — 다른 c에서도 cliff가 10~15 사이인지는 미확인. pool=11~14 같은 더 촘촘한 탐색은 안 함(이 정도 해상도면 서사상 충분하다고 판단, 추가 정밀화는 비용 대비 실익 낮음).
- 이 EC2 2대는 테스트 종료 후 삭제(과금 방지) — 재현하려면 인프라를 다시 띄워야 함.

> 결과 파일: `pool10-c*.json`, `pool30-c*.json` (로컬 세션에만 보관, 저장소에 커밋 안 함 — 인프라도 함께 삭제됨).

---

## 6. 추천 (2026-06-12 측정, 2026-07-25 분리 배포 실측 반영 — 결정 전)

측정이 가설을 **세 번 수정**시킴: ① "행수가 아픔" → 아니다(풀/박스) ② "풀=10이 캡" → 아니다(박스, *로컬 동거 환경 한정*) ③ "풀 사이징은 천장 해법 아님" → **분리 배포에서는 그렇지 않다**(§5-1(7), 고부하 c≥50에서 풀=30이 풀=10 대비 확실히 낫고, c=100에선 풀=10이 붕괴). 즉 (5)의 "박스가 천장" 결론 자체는 *그 로컬 실험 안에서는* 유효하지만, "풀 사이징이 무용하다"는 일반화는 **동거 환경에 종속된 결론**이었다 — 분리 배포에서는 풀이 다시 유효한 레버가 된다.

| 레버 | 측정 결과 | 결론 |
|---|---|---|
| 풀 사이징(10→30), 로컬 동거 | RPS 개선 0, p99 악화 | ❌ 이 **환경 한정** 천장 해법 아님(박스-바운드) |
| 풀 사이징(10→30), **분리 배포**(§5-1(7)(8)) | c=100 기준 pool=10 붕괴(RPS 5.9·47%실패), pool=15/20/30은 전부 동급(RPS 190~200대·실패0) | 🟢 **분리 배포에서는 진짜 레버, 단 실측 스위트스폿은 ~15**(cliff는 10~15 사이). 이론 공식(≈5)보다 3배 크고 30까지는 불필요 |
| 다운샘플(R 25→5) | RPS +126%·p99 −70%(c=50), 저장행 5배↓ | 🟢 **진짜 레버** — 배치당 W를 줄여 처리량·지연·저장 동시 개선. 단 R≈5 스위트스폿(R=1 과도) |
| 수평확장 / 코어↑ | (미측정) | 박스가 로컬 천장이므로 절대용량의 답. 단 DAU 규모상 당장 불요 |

> 핵심 한 줄(최종): **"로컬에서는 쓰기 천장이 행수도 풀도 아닌 2코어 박스였다(풀 30도 무효) — 그런데 분리 배포로 실측해보니 그 '풀 무효' 결론 자체가 박스 공유의 부산물이었다. DB가 전용 코어를 가지면 풀 사이징은 고부하 구간(c≥50)에서 확실히 유효하다. 다운샘플은 환경과 무관하게 배치당 비용을 줄여 처리량 +126%·저장 5배↓를 주는 레버."** — *가설→측정→반증을 세 번 돌려 레버를 재배치*한 게 서사 핵심.

**다운샘플 착수 판단**: 성능(쓰기 천장)이 아니라 **저장 감축 + 배치 효율 + 헤드룸** 목적이면 가치 있음. 위치 B(Spring 대표추출), R 하한 ~5, 리포트 해상도 SLA 확인 후. "성능 처방"으로 과장 금지.

---

## 7. 미결정 (사용자 confirm 필요)

- [x] **다운샘플 착수 여부**: **착수함**(2026-07-25, PR #53) — 저장 감축·배치 효율 목적으로 위치 B(Spring 대표추출) 구현. `PoseDataService.savePoseDataBatch`에서 R=5 윈도우별 sync_rate 최저 프레임만 저장. 성능 천장 목적 아님(박스-바운드)이라는 전제는 유지.
- [x] **방식/위치**: **B(Spring 대표추출) 채택, R=5 하한 준수**하여 구현 완료(위와 동일 커밋)
- [x] **리포트 해상도 SLA**: **현재 기능 기준 리스크 없음으로 확인(2026-07-25)** — 리포트가 pose_data 시계열을 쓰는 곳은 `WorstSectionCalculator`(연속 3행 윈도우로 "가장 안 좋았던 순간" 1건, 타임스탬프+텍스트만 반환) 하나뿐. `syncRateDetails`는 pose_data가 아니라 session.avgSyncRate(별도 집계)를 씀. 프레임 단위 타임라인/그래프는 현재 어디에도 없음 — R=5 오차(~1초 미만)는 이 텍스트 요약엔 무의미.
- [ ] **raw 버퍼 TTL(§2-0)과의 관계 / 미래 타임라인 기능 확장성 — 의도적으로 보류(YAGNI, 2026-07-25)**: 위치 B는 대표 프레임 외 나머지를 디스크에 쓰지도 않고 버리는 구조라 **한 번 저장된 세션은 원본 밀도로 복구 불가**. 게다가 지금 대표추출 전략(윈도우당 sync_rate 최저값 선택)은 "가장 안 좋았던 순간 찾기"엔 맞지만, 나중에 시계열 그래프를 만들 때 쓰면 매 포인트가 그 구간의 최솟값이라 **실제보다 계속 나빠 보이는 편향된 그래프**가 나옴. 지금은 그런 기능이 계획에 없어 착수하지 않지만, 나중에 타임라인/그래프 기능이 실제로 로드맵에 오르면 그 시점에: (1) 그 이전 세션은 복구 불가함을 감안, (2) 대표추출 전략을 최솟값이 아닌 균등샘플/평균으로 바꿀지 재검토할 것.
- [x] (선택) 절대천장이 필요하면 **부하기 분리 + 현실 코어 VM** 환경 마련 — §5-1(7)(8)에서 EC2 2대로 수행 완료. pool=15/20까지 c=100 기준 실측해 cliff를 10~15 사이로 좁힘(15가 20/30과 동급). 남은 미탐색: pool=11~14(cliff 정밀 위치)와 c=100 외 다른 동시성 레벨에서의 cliff 재현 여부

---

## 결정 로그
- 2026-06-12: 문서 작성. roadmap §10-③의 "AI vs Spring"을 §2-0(raw=버퍼) 진화 반영해 재구성 — *영속 공간 명분 폐기, 라이브 적재 비용으로만 정당화*. 위치 A/B/C 비교, **C(측정 먼저)→필요 시 B(Spring 대표 추출)** 추천. **착수·위치·방식 미결정**(§7).
- 2026-06-12: **측정 수행(§5-1 (1)~(4))**. baseline 포화 → 버퍼풀 가설 반증(디스크 +2), 단일세션 꼬리지연 아티팩트, R-sweep 고정비용 지배.
- 2026-06-12: **풀 사이징 실측(§5-1 (5))**. 풀 10→30 재생성 후 동일 스윕 → **RPS 개선 0·p99 악화 = 천장은 풀 아닌 박스(2 물리코어)**. 가설 2차 반증(풀=10도 캡 아님). 추천 최종 개정 — **다운샘플은 +126%·저장5배↓의 진짜 레버**(앞 "부수카드" 정정), 단 절대천장은 동거측정이라 무의미·박스/수평확장이 답. 다운샘플 착수는 *저장·배치 목적*으로만 가치, 미결정.
- 2026-07-11: 사용자 질의("스레드풀이 로컬 CPU에 의존해?")로 §5-1(5) 결론의 근거를 재점검 — HikariCP 스위트스폿 공식이 원래 가리키는 건 DB 서버 코어수인데, 로컬 실험은 mysql·backend가 코어를 공유해 "박스 병목"으로 뭉뚱그려졌던 것을 정정(취소선 없이 추가 정정으로 기록, 기존 결론 자체는 유효). §5-1(5) 정정 반영. 이어서 §5-1 (6) 추가 — 분리 배포(RDS) 가정 시 재계산을 사용자 confirm 거쳐 이론 계산으로 정리(`db.t4g.micro`=2vCPU 가정, 공식상 5). 실제 RDS는 안 띄움(비용 발생, 착수 여부 미결정) — 이론 계산이 실측과 다를 수 있다는 한계도 명시.
- 2026-07-25: **분리 배포 실측(§5-1(7))**. 사용자 승인 하에 AWS EC2 임시 인스턴스 2대(DB 전용 + 백엔드/ghz)로 (6)의 이론계산을 실측 검증. RDS 대신 자체관리 MySQL on EC2 사용(RDS는 IAM 권한 이슈로 EC2로 전환, 사용자 확인). 로컬(5)와 동일한 풀 10→30 비교를 분리 배포에서 재현한 결과 **c≥50에서 결론이 반전** — 풀=30이 풀=10 대비 확실히 우세하고, c=100은 풀=10이 완전히 무너짐(47% DeadlineExceeded). "풀 사이징 무용"은 로컬 동거 환경에 종속된 결론이었음을 실측으로 확인. c=5 이상현상(재현성 있게 나쁨)은 이 실험의 sweep 스크립트 warmup(c=20)이 pool=10보다 커서 생긴 측정 아티팩트일 가능성으로 정정(가설 단계, 미확정). 실험 중 EC2 메모리 설정 실수(호스트 RAM만 올리고 컨테이너 cgroup 캡을 안 풀어 OOM 반복) 두 건 발생·수정 — 방법론적 함정으로 정직하게 기록. §6/§7 갱신. 인프라는 실측 후 삭제 예정(과금 방지).
- 2026-07-25: **풀 사이징 cliff 확인(§5-1(8))**. 사용자 요청으로 (7)의 10/30 두 점 사이를 좁히기 위해 pool=15·20을 c=100 기준으로 추가 측정(warmup을 c=10으로 낮춰 (7)에서 의심한 아티팩트 회피). 결과: **cliff는 10~15 사이**, 15부터는 20·30과 사실상 동급(실패 0, RPS 190~200대) — 30까지 늘릴 필요 없이 실측 스위트스폿은 **~15**로 좁혀짐. 이론 공식(≈5)보다 3배 크다는 점도 확인, 워크로드(단순 batch INSERT, I/O·락 대기 비중↑)에서는 CPU 코어 기준 공식이 과소평가한다는 근거로 기록.
- 2026-07-25: **다운샘플 실착수 + 풀 사이징 코드 반영(PR #53)**. §7의 "다운샘플 착수 여부"·"방식/위치" 미결정 두 항목을 사용자 confirm 거쳐 실제로 착수 — `PoseDataService.savePoseDataBatch`에 위치 B(Spring 대표추출) 구현(윈도우=5, sync_rate 최저 프레임만 저장), `application.yml`에 `maximum-pool-size: 15` 반영. CodeRabbit 리뷰로 `application.properties`에 남아있던 구버전 `maximum-pool-size=10`이 같은 위치 우선순위(properties > yml)로 yml 설정을 조용히 무시하던 버그 발견·수정(Major) — 로드맵·본 문서 §7의 doc-sync 누락도 함께 지적받아 수정(Minor). §7 갱신: 남은 미결정은 리포트 해상도 SLA(도메인 확인)와 raw 버퍼 TTL 관계뿐.
- 2026-07-25: **리포트 해상도 SLA 확인 + raw 버퍼 TTL 항목 의도적 보류**. 코드 조사 결과 리포트가 pose_data 시계열을 쓰는 곳은 `WorstSectionCalculator`(연속 3행 윈도우 → 타임스탬프+텍스트 1건) 하나뿐이고 `syncRateDetails`는 pose_data가 아닌 session 집계값을 씀 — 프레임 단위 타임라인/그래프가 현재 존재하지 않아 R=5 오차(~1초 미만)는 무의미, **SLA 항목 해소**. 이어서 "나중에 타임라인 기능을 추가하면?" 질의에 답하며 raw 버퍼 TTL 항목을 재구성 — 위치 B는 대표 외 프레임을 디스크에 쓰지도 않고 버려 **한 번 저장된 세션은 원본 밀도 복구 불가**, 게다가 현재 전략(윈도우당 sync_rate 최솟값)은 미래 시계열 그래프 용도로 쓰면 실제보다 나빠 보이는 편향된 그래프를 만듦. 지금 그런 기능이 계획에 없어 YAGNI로 의도적 보류, 실제 로드맵에 오르면 그때 재검토하기로 함(사용자 confirm).
