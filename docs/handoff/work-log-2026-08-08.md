# 작업 기록 — 2026-08-08

범위: Spring 백엔드. 관련 PR [#123](https://github.com/Shadowfit/init/pull/123)(머지) · [#125](https://github.com/Shadowfit/init/pull/125) · [#126](https://github.com/Shadowfit/init/pull/126), 이슈 [#124](https://github.com/Shadowfit/init/issues/124).

---

## 0. 한 줄 요약

**#3(관리자 페이지) 백엔드를 닫고 #6(관측 스택)을 세웠다. 그런데 이날 가장 값진 산출물은 기능이 아니라 "틀린 것을 세 번 찾은 것"이다** — 계획 문서가 남았다고 적은 일 2건이 이미 끝나 있었고, 관측 스택 설계에서 내 추측이 두 번 뒤집혔다. 셋 다 **문서나 추론이 아니라 실행으로** 드러났다.

---

## 1. #3 관리자 페이지 — 잔여 2건이 이미 끝나 있었다

착수하러 갔더니 [`../tasks/28-remaining-work-plan.md`](../tasks/28-remaining-work-plan.md) §3 이 잔여로 적어둔 두 항목이 모두 완료 상태였다.

| 잔여로 적혀 있던 것 | 실제 |
|---|---|
| B 필터 조합 EXPLAIN 🔴 미측정 (2~4h) | ✅ [`../decisions/admin-page-scope.md`](../decisions/admin-page-scope.md) **§4-4**(08-04, 조합 5종) + **§4-4-1**(08-06 재측정) |
| 공통 페이징 DTO·예외·Swagger 📋 미착수 (2~3h) | ✅ `dto/common/PageResponse.java` + 컨트롤러 Swagger 애노테이션 |

### 두 오판의 원인이 서로 다르다

- **B** — §4-1 "측정하지 않은 것" 3번의 괄호(*"세션 목록 B 쪽 조합은 여전히 미측정"*)를 인용했다. 그 문장은 작성 시점엔 참이었으나 **같은 날 §4-4 가 측정을 끝내면서 거짓이 됐고**, 괄호만 남았다. **한 문서의 낡은 각주가 다른 문서의 남은 일이 됐다.**
- **공통 DTO** — `dto/admin/` **한 디렉터리만 보고** 없다고 판정했다. 실제 위치는 `dto/common/` 이다.

### 📌 남는 교훈 — 근거의 등급이 항목마다 달랐다

바로 하루 전(08-07) 정정도 *"코드로 확인했다"* 고 적었다. 그런데 실제로 확인한 것은 **컨트롤러·서비스 파일의 존재**였고, 이 두 항목은 **문서의 자기서술**과 **한 디렉터리**로 판정했다. 등급이 다른 근거를 표는 구분 없이 적었다.

> 08-07 정정은 08-03 판이 과대하다고 고친 것이었는데, **그 정정 자체도 과대했다.** 같은 표가 이틀 연속 과대로 정정된 셈이다.

**#3 백엔드는 이로써 닫힌다.** 남은 것은 C(운동/영상 CRUD, 보류)와 관리자 프론트(추정 없음)뿐이다.

---

## 2. 진짜 구멍 하나 — 400 대신 500 ([#124](https://github.com/Shadowfit/init/issues/124), PR [#125](https://github.com/Shadowfit/init/pull/125))

위 표의 **"예외"** 칸이 가리키던 자리에 실제 결함이 있었다.

```
GET /admin/sessions?sort=BOGUS  → 500
GET /admin/members?sort=BOGUS   → 500
GET /admin/sessions?page=abc    → 500
```

`GlobalExceptionHandler` 가 `MethodArgumentNotValidException`(`@Valid` 실패)만 400 으로 잡는다. 그런데 **관리자 목록은 `@Valid` 를 안 쓴다** — 필터가 전부 선택 항목이라 검증할 제약이 없고, 대신 **타입 변환**이 검증 역할을 한다. `sort=BOGUS` 는 enum 에 없어 `MethodArgumentTypeMismatchException` 으로 터지는데 핸들러가 없어 `Exception.class` 로 떨어졌다.

**같은 클래스에서 한 번 있었던 버그와 형태가 같다.** `GlobalExceptionHandler.java:29-33` 주석이 `AccessDeniedException` 이 핸들러가 없어 403 대신 500 으로 나가던 건을 기록해뒀다. 그때는 403 이, 이번엔 400 이 삼켜졌다.

### 같은 엔드포인트에서 400/500 이 갈리고 있었다

검색조건 record 안의 필드(`status`·`startedFrom`)는 **이미 400 이었다** — Spring 6.1 부터 `@ModelAttribute` 바인딩 실패가 `MethodArgumentNotValidException` 으로 오기 때문이다. 즉 **파라미터를 어디에 선언했느냐**에 따라 결과가 달랐다.

고친 뒤 `AdminQueryParamBindingErrorTest` 5건으로 고정했다(수정 전 3건 실패). 이미 통과하던 2건도 함께 넣었다 — 프레임워크가 바꾸면 알아채야 하는 지점이라서.

---

## 3. #6 관측 스택 — Prometheus + Grafana (PR [#126](https://github.com/Shadowfit/init/pull/126))

### 착수 조건은 이미 충족돼 있었다

계획 문서는 *"남은 실험을 하기로 정할 때만. 안 하면 값이 없다"* 는 조건을 달아뒀다. 그 전제는 **"볼 것이 없다"** 였는데, 실제로는 계측이 **9종** 박혀 있고 **볼 수단만** 없었다. 그중 절반은 시계열이라야 답이 나온다 — 적체는 *기울기*, 고아 창은 *꼬리(p99)*, 락 충돌은 *부하와의 상관*이 답이다.

> 계획 문서가 "커스텀 메트릭 3종"이라고 적고 있었다. 3종은 **2026-07-28 시점**의 수(상태 전이·낙관락 충돌·배치 행수)이고, 이후 아웃박스 3·pose 고아 2·AI stop 1 이 더해져 **9종**이다. 현재 상태를 서술하는 문장만 고쳤고 **날짜가 박힌 기록은 손대지 않았다.**

### 구성

| 결정 | 내용 |
|---|---|
| 액추에이터 노출 | **관리 포트 9090 분리**. prod compose 는 매핑하지 않고 dev 만 로컬 검증용으로 연다 |
| 기동 | **compose profile `obs`** — 기본으로 안 뜬다. 2물리코어 장비에서 부하 실험과 동거하면 측정이 오염된다 |
| 대시보드 | **JSON 커밋(프로비저닝)**. UI 로 만들면 볼륨을 날렸을 때 재현이 안 되고 리뷰도 안 된다 |
| 보존 | 7일 |

상세는 [`../../monitoring/README.md`](../../monitoring/README.md).

### 🔴 추측이 두 번 틀렸다 — 둘 다 실행해서 알았다

**① 시큐리티가 관리 포트에도 걸린다.**

포트를 나누면 시큐리티가 거기엔 안 걸릴 것으로 봤다. 아니었다 — **필터체인이 두 포트에 공유된다.**

```
분리만 했을 때:  9090 /actuator/prometheus → 401   ← Prometheus 가 못 긁는다
                 9090 /actuator/health     → 200   ← whitelist 에 있어서
```

그대로 뒀으면 스크레이프가 **"타깃 DOWN"으로 조용히 실패**했을 것이다. whitelist 에 넣어 해소.

결과적으로 **보호 수단이 "인증"이 아니라 "포트 경계" 하나로 분명해졌다.** 8080 에 같은 경로를 쳐도 지표는 안 샌다(500 + 일반 오류 본문, 지표 문자열 없음). 🔴 **9090 을 노출하는 순간 whitelist 가 그대로 뚫린다.**

**② 지표 9종 중 2종만 나온다.**

Micrometer 는 카운터·타이머를 **첫 기록 시점에** 만든다. 갓 띄운 서버에는 게이지 2종(`shadowfit_outbox_pending`·`shadowfit_pose_orphan_rows`)만 존재한다.

그래서 **대시보드 쿼리 이름이 맞는지를 서버로는 확인할 수 없다** — 없는 지표는 이름이 틀려도 똑같이 안 나온다. `SessionMetricsExportNamesTest` 3건으로 이름 10개·태그·`quantile` 라벨을 고정했다. **깨지면 대시보드 JSON 도 같이 고쳐야 한다.**

### 실측 확인 범위

```
스크레이프 타깃      shadowfit-backend:9090/actuator/prometheus → up
그라파나 → 프로메테우스  "Successfully queried the Prometheus API"
프로비저닝           데이터소스(shadowfit-prom) · 대시보드(Shadowfit 폴더)
그라파나 경유 쿼리     hikaricp_connections_pending{application="shadowfit"} → 값 반환
8080 비유출          /actuator/prometheus → 500, 지표 문자열 없음
전체 백엔드 스위트     BUILD SUCCESSFUL
```

### 🔴 미검증 · 알려진 흠

- ~~**부하를 넣은 상태의 그래프는 아직 안 봤다.**~~ 이 스택의 1순위 용도(풀 붕괴의 과정)는 rig 재실행이 남았다 → ✅ **같은 날 저녁 해소, 단 «붕괴» 는 없었다 (§7-1)**
- **8080 `/actuator/health` 가 404 대신 500** — whitelist 가 두 포트 공유라 그 줄을 빼면 9090 헬스체크가 401 로 깨진다. 감수
- **관리 포트에 인증이 없다.** 네트워크 경계가 유일한 보호 수단
- 세션 전이·락 충돌·아웃박스 발행·다운샘플 R 패널은 **세션이 한 번도 안 돌아 비어 있다.** 고장이 아니다

---

## 4. 고친 문서

| 문서 | 무엇을 |
|---|---|
| [`../tasks/28-remaining-work-plan.md`](../tasks/28-remaining-work-plan.md) | §0 요약 · §2 표 · §3(잔여 2건 정정) · §2-6(관측 스택 착수 + "3종→9종") |
| [`../decisions/admin-page-scope.md`](../decisions/admin-page-scope.md) | §4-1 "측정하지 않은 것" 3번의 낡은 괄호 — 이게 오판의 뿌리였다 |
| [`../portfolio/portfolio-narrative.md`](../portfolio/portfolio-narrative.md) | 관측성 행 — 지표 9종 + 시계열 |
| `.claude/skills/verify/SKILL.md` | health URL 8080 → 9090, obs 프로파일 안내 |

---

## 5. 남은 결정 (사용자 몫)

> 🔄 **이 목록은 작성 시점 기준이다. 같은 날 저녁에 1번이 실행됐고 3·4번의 성격이 바뀌었다 — §7 참조.** 목록 자체는 그대로 둔다(그 시점의 판단 기록이라서).

- [ ] **부하 rig 재실행** — 설계가 아니라 실행. 이 스택을 세운 이유가 아직 안 채워졌다. ⚠️ 2코어라 **관측을 켠 채 부하를 주면 측정이 오염**되므로 "수치"와 "그래프" 중 하나를 골라야 한다 (추천: 그래프 — 수치는 이미 있다)
- [ ] **판단 기준선(SLO) 문서** — 그래프는 생겼는데 *"얼마면 나쁜 건가"* 가 없다. 특히 고아 행(#87)은 코드 주석이 *"발생 빈도를 모르면 핫패스에 락을 얹을 만한가를 판단할 근거가 없다"* 고 적어놨는데, 셀 수단은 생겼고 **몇 건부터 고칠지는 여전히 미정**이다
- [ ] **mysqld_exporter** — 초안에서 *"`SHOW STATUS` 와 중복"* 이라 제외했으나 **근거가 약하다.** 그건 실험 때 손으로 돌리는 것이고, 아래 쿼리들은 관리자가 화면을 열 때마다 도는 상시 경로다. 🔶 열림으로 되돌림
- [ ] **prod 에 관측 스택을 올릴지** — 지금은 dev compose 에만 있다. AWS 가 붙는 #4 와 같은 시점에 정하는 게 맞다. ⚠️ 호스트 RAM 만 올리고 `docker --memory` 캡을 안 풀면 그대로 OOM 인 전례가 있다
- [ ] **#5 외부 통합** — S3 로 기운 상태이나 **확정 아님**. 대상(프로필 이미지 등)과 방식(presigned URL vs 서버 경유)이 미정

### 참고 — 이 프로젝트의 무거운 쿼리 (기준선 문서의 재료)

| 쿼리 | 실측 | 출처 |
|---|---|---|
| D 대시보드 `b` 상태별 분포 | **425ms**(EXPLAIN ANALYZE 중앙값), 100만 행 | `admin-page-scope.md` §4-5 ② |
| D 대시보드 `e` 활성 회원 | **0.63s**, 100만 행 | 〃 |
| 관리자 목록 총건수 | 대부분 조합에서 **20만 행 풀스캔** | §4-3 ③ |
| B 세션 목록 | **5/5 조합** `temporary+filesort` | §4-4 ① |

대시보드 비용의 **98% 가 `b`·`e` 둘**이고 나머지 셋은 합쳐 0.02초다. 집계 전략(㉲)이 §4-5 에서 미결로 열려 있고, **관측 기준선과 그 결정이 같은 자리에 있다.**

---

## 6. 로컬 환경 메모

- 이날 검증 중 **8080 을 다른 프로젝트(`dockin-app-1`)가 점유**하고 있어 호스트 포트를 임시로 비켜 확인했다(`18080`/`19090`). `docker compose up` 이 그대로는 실패한다
- 검증이 끝난 뒤에도 관측 스택은 **띄워둔 상태**다. 끄려면 ~~`docker compose --profile obs down`~~
  > 🔴 **이 명령을 쓰지 말 것** ([#131](https://github.com/Shadowfit/init/issues/131), `8d4f49b`). `--profile` 은 **올릴 대상을 고르는 옵션이고 내릴 대상을 좁히지 않는다** — `down` 은 백엔드·AI·MySQL 까지 정지 후 **제거**한다(`--dry-run` 확인). 관측만 끄려면 서비스를 명시한다:
  > ```bash
  > docker compose --profile obs stop prometheus grafana        # 끄기 (관측만)
  > docker compose --profile obs rm -sf prometheus grafana      # 컨테이너까지
  > ```
  > 📌 **원문을 취소선으로 남기는 이유**: 이 문서는 날짜가 박힌 기록이라 지운 것처럼 보이면 안 되지만, 이 줄만은 **사실 서술이 아니라 지시문**이라 그대로 두면 읽는 사람이 그대로 친다. 최신 안내는 [`../../monitoring/README.md`](../../monitoring/README.md).

---

## 7. 🔄 같은 날 저녁 후속 — §5 목록이 하루도 못 갔다

이 문서를 쓴 뒤 같은 날 저녁에 §5 의 1번이 실행됐다. **§5 를 쓸 때 예상하지 못한 것이 두 개 있다.**

### 7-1. 1번(부하 rig 재실행) — 실행됐고, 전제가 틀렸다

§5 는 *"2코어라 «수치»와 «그래프» 중 하나를 골라야 한다 (추천: 그래프)"* 고 적었다. **그 택1이 성립하지 않았다** — 로컬이 아니라 **EC2 3대(app·db·obs 분리)** 로 돌려 둘 다 같은 판에서 얻었다. obs 를 자기 인스턴스에 두면 App·DB 코어를 안 먹는다는 것이 `monitoring/README.md` 에 이미 적혀 있었는데, §5 는 로컬을 전제로 딜레마를 만들고 있었다.

**그리고 보러 갔던 것이 없었다.** 이 스택을 세운 1순위 명분은 *"pool=10·c=100 이 47% 타임아웃으로 붕괴한 그 순간의 그래프"* 였는데, 재측정에서 **절벽이 아예 없었다**(pool 5~20 × c 10~100 전 구간, 실패 0).

| 왜 | 근거 |
|---|---|
| ~~다운샘플(R=5)이 병목을 **DB 쓰기 → 백엔드 CPU** 로 옮겼다~~ | ~~부하 중 백엔드 p90 **128%** vs MySQL **53%**~~ → 🔴 **철회** (아래) |
| **풀은 병목이 아니다** | `pool=20` 은 20개 중 **2~3개**만 사용(대기 0), `pool=5` 는 **포화**(상한+95 대기)인데 **RPS 동일**(203.9 vs 208.9) |
| 그래서 47% 붕괴는 **옛 코드(배치 25행)의 현상**이다 | `DOWNSAMPLE_WINDOW` 를 1 로 되돌린 대조군: RPS **1.7배**·p99 **4.9배** 차이 |

> 📌 **이 문서의 §0 이 말한 "틀린 것 세 번"에 네 번째가 붙는다.** 앞의 셋은 문서·추측이 틀린 것이었는데, 이번엔 **우리가 넣은 처방(R=5)이 우리 벤치마크의 근거(pool=15)를 무효화한 것**이다. PR #53 이 두 레버를 같이 넣었고, 앞의 것이 뒤의 것을 지웠다 — 그걸 3개월 뒤에 알았다.

**관측 스택의 값어치는 다른 데서 나왔다.** `pool=5·c=100` 에서 `pending` **95** · 획득 대기 **0.96초**인데 `timeout_total` 0 — **포화와 붕괴가 다르다**는 것을 보여줬다. 수치만 보면 "실패 0" 하나로 *한가한 것*과 *포화인데 버티는 것*이 구분되지 않는다.

> 🔴 **정정 — 같은 날 저녁 코드리뷰 (§7-4).** 이 절의 초판은 위 두 표와 이 문단에 **틀린 수치**를 적었다. `pending 96`·획득 대기 `7.28초`·`active 20` 은 **전부 스윕 밖 구간**(옛 코드 R=1 대조군·v1)의 값이었고, 백엔드 `128%`/MySQL `53%` 는 **커밋된 원본에 없다** — MySQL 지표는 아예 수집되지 않았고 스윕 중 백엔드 CPU 는 0.51~0.56 이었다. 그래서 **«병목은 백엔드 CPU» 는 철회**했고, ~205 RPS 천장의 정체를 **미상**으로 남겼다(1순위 용의자: 부하기 커넥션 1개).
> ✅ **후속 — 천장의 정체가 밝혀졌고, 그 «1순위 용의자» 는 기각됐다** (2026-08-08 저녁, [`../../loadtest/results/ceiling-fsync-2026-08-08/`](../../loadtest/results/ceiling-fsync-2026-08-08/)).
>
> **답은 커밋 `fsync` 였다.** c=100 · pool=20 · `-n 12000` 고정, MySQL 내구성 설정만 바꿨다:
>
> | `innodb_flush_log_at_trx_commit` / `sync_binlog` | RPS | p50 |
> |---|---|---|
> | `1` / `1` (기본값, 최대 내구성) | **231.6** | 429ms |
> | `1` / `0` | 411.5 | 240ms |
> | `2` / `0` | **803.1** | 121ms |
>
> 커밋마다 fsync 를 두 번(redo 플러시 + binlog 동기화) 돌고 있었고, EBS 가 네트워크 연결 디스크라 그 왕복이 처리량을 결정했다. **3.47배.**
>
> 🔴 **그리고 위에 적은 «1순위 용의자» 는 측정으로 기각됐다** — `--connections` 만 흔들었더니 `1`→**230.2**, `4`→**208.0**, `16`→**210.9** 로 **안 움직인다**(16배로 늘려도 그대로거나 약간 낮다). 세 박스 CPU 도 전부 유휴였다(부하기 13~20% · 백엔드 27~37% · MySQL 50~59%) — **전부 놀면서 처리량이 고정이면 병목은 «계산» 이 아니라 «대기»** 이고, 그래서 커밋 경로를 의심한 것이 맞았다.
>
> 📌 **이것이 오전 관측을 전부 설명한다.** 풀 5 vs 20 이 같았던 것은 직렬화 지점이 커넥션이 아니라 **로그 플러시**였기 때문이고, `pool=20` 에서 2~3개만 쓰인 것은 커넥션당 DB 작업이 짧고 시간이 **커밋 대기**에 있었기 때문이다. 다운샘플이 절벽을 지운 이유도 같다 — R=1(25행)일 때는 커밋당 행 작업이 커서 풀이 레버였는데, R=5 로 줄자 **행 작업이 fsync 비용 아래로 내려갔다. 커밋 «횟수» 는 그대로다.**
>
> ⚠️ **채택하지 않았다.** 3.47배는 **데이터 안전을 판 대가**이고 DAU 1,000 가정에서 231 RPS 는 한참 위다. 안 아픈 것을 고치며 안전을 깎는 셈이라 «천장의 위치를 안다» 로 닫았다. 내구성을 안 깎는 유일한 레버(여러 rep 을 한 트랜잭션으로 묶어 **커밋 횟수**를 줄이기)는 미측정이다.
>
> ⚠️ `conn` 스윕 3판은 `fail=100` 이 찍혀 있다 — 지속시간 모드(`-z`) 종료 시 in-flight 가 끊기는 **알려진 아티팩트**(`load-test-strategy.md` §7.7)이고 판정에는 영향이 없다.
>
> ✅ **그리고 그날 밤 AWS 를 다시 띄워 답을 냈다 — 커밋 `fsync` 였다**([`../../loadtest/results/ceiling-fsync-2026-08-08/`](../../loadtest/results/ceiling-fsync-2026-08-08/)). 내구성 설정만 바꿔 **231.6 → 803.1 RPS (3.47배)**. 1순위 용의자는 반증됐다(`--connections` 1→16 무변화). 🔴 **채택은 안 했다** — 배수의 대가가 데이터 안전이고 목표 규모 대비 231 RPS 는 여유라서, *"천장의 위치를 안다"* 로 닫았다.
>
> 📌 **이날 «틀린 것» 이 최종 여섯이 됐다.** 그중 마지막 둘은 성격이 다르다 — **내가 낸 측정 결론이 틀렸고**(수치 오귀속), **그 정정에서 세운 가설도 틀렸다**(부하기). 둘 다 원본을 커밋해뒀기 때문에 대조가 가능했다.
>
> 📌 **§0 의 "틀린 것 세 번"에 다섯 번째까지 붙은 셈이다.** 그런데 성격이 다르다 — 넷째까지는 *"문서·추측이 틀렸다"* 였고, **다섯째는 «내가 방금 낸 측정 결론이 틀렸다»** 이다. 원본을 커밋해뒀기 때문에 대조가 가능했고, 그게 39개 파일을 커밋한 값어치였다.

원본: [`../../loadtest/results/pool-cliff-2026-08-08/`](../../loadtest/results/pool-cliff-2026-08-08/) (39개 커밋 — 지난번에 안 남겨서 같은 질문에 두 번 돈을 낸 것이 이 결정의 이유다) · 결론 반영: [`../decisions/pose-ingest-downsampling.md`](../decisions/pose-ingest-downsampling.md) §5-1(9)

### 7-2. 2번(SLO)·3번(mysqld_exporter) — 근거가 바뀌었다

- **2번 SLO** — 여전히 없다. 다만 이번 실험이 **판정선이 없으면 실험 설계가 막힌다**는 것을 실물로 보여줬다: cliff 판정에서 "p99 가 SLO 초과" 안을 **쓸 수 없어** 임의값(plateau 90%)을 미리 못박는 방식으로 우회했다. 🔴 우선순위를 올릴 근거가 하나 생겼다
- **3번 mysqld_exporter** — 🔶 열림. ~~*"이번 실험이 근거를 약화시켰다 — MySQL 이 p90 53% 로 놀고 있었다"*~~ → 🔴 **정반대로 뒤집혔다.** 그 53% 는 **수집된 적 없는 수치**였다. `metrics/` 10종이 전부 백엔드 것이라 **"MySQL 이 놀았다"를 말할 근거가 애초에 없었다.** 즉 이번 실험은 mysqld_exporter 의 **필요를 오히려 입증**했다 — 없어서 결론을 못 냈다. 🔴 **우선순위 올림**

### 7-3. §5 에 없던 것이 새로 열렸다

- 🔴 **`maximum-pool-size` 값 재검토** — 15 가 해롭다는 증거는 없으나 근거가 낡았다. [`../decisions/pool-cliff-vs-concurrency.md`](../decisions/pool-cliff-vs-concurrency.md) §8
- 🔴 **재측정 설계에 «측정 대상 코드가 그새 바뀌었나» 를 넣는다** — 이번 설계는 그 질문을 한 번도 하지 않았다
- ⚠️ **부하기 위치가 원칙에 빠져 있었다** — `load-test-strategy.md` §2 가 "tier 격리"를 원칙으로 세웠는데 부하기 자신은 대상으로 안 적어둬서 두 실험 연속 백엔드와 동거시켰다. §7.6 에 표로 박았다
